# Decisions

Running log of non-obvious development choices. Each entry: the choice, the rationale, and (when relevant) the scale at which it must be revisited.

When a decision is refined, edit its entry in place so it always describes the current choice. When a decision is fully superseded, delete the stale entry rather than layering "supersedes" notes on top of it. The log should read as what is true now, not as a history of what changed.

Keep every entry skimmable: 1-3 sentences per field. Config defaults live in `config.ts` / `.env.example`, not here; alternatives considered and rejected get a phrase, not a paragraph. If an entry starts running long, it is drifting from the point of this file. A decision that is purely local tooling detail (a workaround for a bug in a library or a dev machine, with no product or architecture consequence) belongs as a code comment where the workaround lives, not here.

## Format

```
### YYYY-MM-DD, track, short title
Choice: what was decided.
Why: the reason.
Revisit when: trigger that should force a rethink (optional)
```

---

## Revisit-later index

Quick scan of choices with a genuine open trigger: something not yet resolved and still worth checking. Most are gated on one open ROADMAP backlog item (firewalling the origin), on live public traffic to tune a default against, or on a look by eye at the real photo the board now serves.

- [Alpha topology](#2026-05-18-infra-deploy-alpha-topology), [WS hardening](#2026-05-18-backend-realtime-ws-hardening), [soak-test IP spoofing](#2026-06-19-qa-and-load-soak-test-state-corruption-validator-and-per-bot-ip-spoofing) -> one open ROADMAP backlog item: the origin still isn't firewalled to Cloudflare ranges. Confirmed still exploitable 2026-08-05 (`ufw`/iptables allow 80/443 from anywhere; the origin answers a direct request to its bare IP for the real `ws.*` hostname); `CF-Connecting-IP` is spoofable until this closes, bypassing every per-IP limit (login, WS, admission queue).
- [Server-computed minimap grid](#2026-06-06-backend-realtime-server-computed-minimap-grid), [per-tile piece cap](#2026-06-11-backend-realtime-per-tile-piece-cap-on-non-merging-drops), [hot-tile residency](#2026-06-05-frontend-canvas-hot-tile-residency-frees-covered-cold-clusters), [stale-hold sweep](#2026-07-05-backend-realtime-grab-reservation-race-and-stale-hold-sweep), [paced region_state batching](#2026-07-06-backend-realtime-paced-region_state-batching), [coalesced standings delta](#2026-08-24-backend-realtime-live-standings-ride-a-coalesced-delta-with-a-personal-standing-for-anyone-outside-the-top-n) -> six unmeasured defaults (24h resync interval, 8x tile-cap multiplier, resident-piece budget/hot TTL, `MPP_STALE_HOLD_MS`, batch size/poll interval, the 2s standings-publish window). All held clean through synthetic soak and lock testing up to real board scale, but none have seen real concurrent public play yet; sanity-check once the event has run a while.
- [A buried piece is not a snap target](#2026-08-30-backend-realtime-a-buried-piece-is-not-a-snap-target) -> the cap of 5 is set against a model of the fresh scatter density, where an accidental refusal is ~2 pieces in a million, and against no measurement of how much players really heap pieces; watch for the toast reading as the game refusing a legitimate snap.
- [Per-tile loading badge removed](#2026-07-28-frontend-canvas-per-tile-loading-badge-removed) -> a not-yet-streamed cell now looks identical to an empty one while zoomed in; revisit only if that reads as a bug during ordinary panning.
- [DZI reveal mask and seam baked server-side via CellCompositor](#2026-07-28-frontend-canvas-dzi-reveal-mask-and-seam-baked-server-side-via-cellcompositor) -> verified pixel-precise at real board scale, but `DZI_QUALITY` (85) is an eyeballed estimate and its trigger has already fired: prod has served the real photo since 2026-08-15 and nobody has judged the reference pyramid by eye on it.
- [Pipeline piece-group reassignment on loose-loose merges](#2026-08-05-backend-realtime-pipeline-piece-group-reassignment-on-loose-loose-merges) -> a large loose-loose merge (as opposed to the singleton-only anchoring path `seed-lock-scenario.ts` exercises) has never run through a soak test; sanity-check if a real merge near `MPP_CLUSTER_PIECE_CAP` is ever observed to lag.
- [The HUD keeps only the reference and the overview on a small screen](#2026-08-06-frontend-shell-the-hud-keeps-only-the-reference-thumbnail-and-the-overview-on-a-small-screen) -> the full-board modal, the scoring explainer and the fit/center controls have no compact entry point left; give them an account-menu entry if any of the three reads as a gap. What stays is still only verified in a dev `/play`, never on a real device.
- [Flat-tile penalty and declustering](#2026-08-13-image-pipeline-tile-selection-penalizes-flat-mostly-empty-black-tiles-and-declusters-the-ones-still-used) -> the 0.5 black-fraction threshold and the two penalty constants are eyeballed against this one tile library; retune by eye if flat tiles still read as too frequent or clustered in the finished mosaic.
- [Umami deployed outside Coolify](#2026-08-16-infra-deploy-umami-deployed-as-an-unmanaged-compose-stack-outside-coolify) -> not yet a Coolify-managed resource, and self-exclusion is operator-only with no public toggle; revisit either if Coolify-dashboard visibility or a visitor-facing opt-out becomes wanted.
- [Load-test bots discover pieces empirically and cluster toward a shared hotspot](#2026-08-18-qa-and-load-load-test-bots-discover-pieces-empirically-and-cluster-toward-a-shared-hotspot) -> the jitter width, cursor ease fraction, and drag-range constants are eyeballed; retune by eye once the first real `--cluster-fraction` watch-mode run gives something to look at.
- [Onboarding skip defaults reuse the existing taken-pseudo retry path](#2026-08-18-auth-and-accounts-onboarding-skip-defaults-reuse-the-existing-taken-pseudo-retry-path) -> the 4-character "Guest #XXXX" tag space (~1.68M combinations) hasn't seen real skip-onboarding volume yet; watch for repeated 409s if usage grows.
- [A restart is announced on the wire](#2026-08-23-infra-deploy-a-restart-is-announced-on-the-wire-and-the-maintenance-page-is-the-apps-own-screen) -> the screen only reacts to an unreachable server; a deliberate pause with its own message still has no way to seal the site, and the 300ms shutdown drain is sized on Docker's default 10s stop grace.
- [Reference underlay mirrors the reveal layer's tiles](#2026-08-24-frontend-canvas-the-reference-underlay-mirrors-the-reveal-layers-own-tiles-and-locked-content-casts-a-shadow-on-it) -> the 0.32 ghost opacity and the locked-slab shadow were both set by eye on a synthetic board whose source image is nearly flat, and a dev stack composites nothing, so the shadow has only ever been seen against one hand-injected baked mask; both want a second look on the real photo.
- [The reference viewers fetch their tiles with CORS](#2026-08-30-frontend-shell-the-reference-viewers-fetch-their-tiles-with-cors) -> the shared browser cache is what made a non-CORS reader poison the board, and the `Vary: Origin` rule that would close it for good is not armed on the assets zone; any future non-CORS reader of an asset URL re-opens the same bug.
- [The update-notes dot seeds a new browser as read](#2026-08-27-frontend-shell-the-update-notes-dot-seeds-a-new-browser-as-read) -> the mark has no expiry and clears only by reading the notes, so a player who never opens the settings menu keeps the dot indefinitely; watch real traffic for whether that reads as nagging.
- [Presence messages](#2026-05-20-shared-protocol-presence-messages) -> nothing on the wire still separates a peer who let go of the mouse from one who left this viewport's cells, which is what the client-side 10s expiry below stands in for; the ROADMAP backlog carries the two messages that would close it.
- [A peer pointer expires after 10s of silence](#2026-08-25-frontend-canvas-a-peer-pointer-expires-after-10s-of-silence) -> the 10s window is eyeballed and trades away an unbounded idle bob; watch a real session for players reading as gone while they are still there, and lengthen it if so.
- [Admission cap set to 200](#2026-08-21-qa-and-load-admission-cap-set-to-200-from-a-measured-single-core-ceiling), [a grab granted after the client gave up](#2026-08-21-backend-realtime-a-grab-granted-after-the-client-gave-up-parks-the-group-for-mpp_stale_hold_ms) -> both wait on real play. The cap was measured against bots that hold a cluster ~85% of the time against a human's 10-30%, so re-measure once the event has run; the late-grant park is only reachable above that cap's own load level and is accepted until real play shows pieces reading as stuck, or until the cap is raised (a redeploy either way, the value is read at boot).

---

## Log

### 2026-05-12, shared-protocol, absolute coords on drag

Choice: `CDrag`/`SDrag` carry absolute `worldX, worldY`, not deltas.
Why: robust to packet loss and reordering, no delta integration on the client.
Revisit when: drag bandwidth shows up in profiling, or per-frame rotation is added.

### 2026-05-12, shared-protocol, drop implies release

Choice: no explicit `release` message; `CDrop` finalizes the position and releases the lock atomically.
Why: one round trip, matches mouse-up semantics.
Revisit when: never expected; a disconnect mid-drag already releases every held group immediately (see per-group dispatch queues below), so no case has needed "user let go" to diverge from "client disconnected".

### 2026-05-12, shared-protocol, PROTOCOL_VERSION

Choice: `PROTOCOL_VERSION` (currently 10) is asserted at handshake. The client sends it in `hello`; the server closes with `protocol_mismatch` on any skew instead of letting a stale client hit malformed-message errors.
Why: turns a client/server skew into one clear handshake error instead of scattered downstream failures.
Revisit when: any further breaking wire change; bump the version again.

### 2026-05-20, shared-protocol, presence messages

Choice: presence is 4 message types. Client to server: `viewport` (visible rect, for broadcast scoping) and `cursor` (pointer). Server to client: `join`, `leave`, and a `cursor` relayed to viewport-neighbor peers; `join`/`leave`/`welcome` also carry `count`, the total connected clients, piggybacked on broadcasts the server already sends (`Hub.clientCount()`, no new message, no added fan-out). There is no server `viewport` relay.
Why: viewport and cursor change on independent triggers (pan/zoom vs pointermove) at different cadences, so a combined message would resend unchanged fields; viewport has no client-facing consumer yet, so relaying it would be speculative. Peer-viewport relay for overview dots was evaluated and rejected: at a coarse-grid, periodic-snapshot resolution cheap enough to broadcast, positions would read stale within seconds next to the exact, real-time peer cursors already shown to viewport-neighbors, so a laggy dot would read as broken tracking rather than a feature; the count alone (exact, free) covers the "people are here" signal without that trade-off.
Revisit when: "pointer left canvas" needs to be distinguished from "peer idle".

### 2026-05-12, piece-generation, canonical sign convention

Choice: each shared edge derives `sign` once from a canonical subseed. The piece using it as `bottom`/`right` takes the canonical sign, the one using it as `top`/`left` takes the opposite; all other params are shared as-is.
Why: both neighbors traverse the edge in the same world direction, so only the sign needs to flip to express "bump out" vs "bump in".
Revisit when: never expected, unless the traversal convention changes.

### 2026-06-09, image-pipeline, pre-cut alpha masks baked server-side

Choice: the slicer bakes each piece's bezier silhouette into its AVIF alpha, and strokes the shared border style into the same tile (`borderBaked: true`), reading per-piece windows directly from the source via libvips random access. The manifest marks `premasked: true`; the frontend renders tiles as-is with no render-time mask or stroke, still deriving geometry from the seed for layout, the spatial index, the overview, and the snap-flash overlay only.
Why: bezier params are frozen, so baking once removes a per-sprite stencil mask (which breaks Pixi batching) from the render hot path, the part that didn't scale to 1M; reading windowed tiles keeps slicer RAM independent of source image size.
Revisit when: the silhouette params or border style change (forces a full re-bake and re-upload of every tile), or rotation is enabled (baked alpha fixes each tile's tab orientation).

### 2026-05-12, image-pipeline, tile margin

Choice: tile margin defaults to `round(0.35 * pieceSize)`, just above the generator's max `depth` param (0.30).
Why: guarantees tabs fit inside the tile with a safety buffer.
Revisit when: edge param ranges widen, or rotation is enabled.

### 2026-05-12, image-pipeline, adaptive pieceSize

Choice: `--piece-size` is optional. When omitted, it is derived from the source image dimensions and the puzzle area is center-cropped.
Why: the pipeline should adapt to any dropped image; center-crop keeps the visually important middle.
Revisit when: a workflow needs the full image with no crop, a non-center anchor, or non-square pieces.

### 2026-05-12, backend-realtime, snap by origin equality

Choice: at drop, the server collects grid-neighbor groups within `snapTolerance`, skips any candidate currently held by another user, picks a target origin (a locked candidate if present), then merges only candidates also within tolerance of that target.
Why: canonical offsets are puzzle-global, so aligned clusters have equal origins; the target-filter pass is needed because two candidates can each be near the dropped group while being up to `2 * snapTolerance` apart from each other. A held candidate is skipped because its stored position is frozen at grab time, not its holder's live position.
Revisit when: rotation is enabled (origins alone won't suffice), or canonical offsets stop being puzzle-global.

### 2026-05-12, backend-realtime, docker all workspace deps

Choice: the server Docker image runs `npm ci --omit=dev` at the repo root (all workspaces), not scoped to `@mpp/server` + `@mpp/shared`.
Why: monorepo workspace-filtered `npm ci` is brittle; installing everything is one deterministic line and still excludes dev deps.
Revisit when: image size or cold-start time matters; switch to a server-only install or split lockfiles.

### 2026-05-12, shared-protocol, piece geometry and seed are server-side only

Choice: piece silhouettes and canonical offsets are never serialized; `generationSeed` is never sent to a client. The client renders pre-masked, pre-bordered tiles plus server-provided `(dx, dy)` member offsets, with no geometry and no seed.
Why: at 1M pieces geometry would dominate payload size; beyond size, a client that could recompute geometry could reconstruct the solution, the reason it closes together with [permuted wire ids](#2026-06-12-shared-protocol-anti-programmatic-solving-via-permuted-wire-ids-and-anchor-offsets).
Revisit when: the generator becomes non-deterministic across platforms; pin to fixed integer math rather than ship geometry. Image-based (CV) solving stays explicitly out of scope.

### 2026-05-12, frontend-canvas, no synchronized entrance cascade

Choice: there is no synchronized event-start cinematic on the canvas. `eventStartsAt` only drives the landing countdown and the `/play` entry gate; once a client is on `/play`, nothing distinguishes "just started" from "already running".
Why: a dedicated cascade animation was never built while the puzzle was small enough for the idea to matter, and was dropped rather than built after the fact for a launch moment that only happens once; the countdown and gate already carry the "something is about to happen" signal.

### 2026-05-29, backend-realtime, per-group dispatch queues

Choice: WS messages dispatch on per-group queues keyed by group id (`GroupQueue`). Disjoint-group tasks run concurrently; same-group tasks run in submission order. `dev_*` admin ops take a global barrier; `hello`/`viewport`/`cursor` run inline; disconnect release is O(held groups) via `Client.held`.
Why: one global serial queue blocked independent groups behind each other; per-group queues keep the ordering handlers depend on while letting disjoint work run in parallel. Keys are acquired synchronously at submission, so the dependency graph can't deadlock.

### 2026-05-18, frontend-shell, dev controls

Choice: `/play` exposes Place/Reset/Complete buttons wired to `dev_*` WS messages, visible under `VITE_DEV_BUTTONS` and accepted server-side only under `MPP_DEV_ENABLED=1`. Reset/Complete require a `confirm()` since they affect every connected tester.
Why: testers need to exercise the end-of-puzzle UI and reset a stuck board with no operator intervention; the env gate lets it be pulled in one redeploy.
Revisit when: flipped off 2026-07-27: `MPP_DEV_ENABLED=0` in `packages/server/Dockerfile`, `VITE_DEV_BUTTONS=0` in `packages/frontend/.env.production`. Local dev keeps both on via an explicit `docker-compose.override.yml` entry (server image is shared with prod; the frontend dev server never reads `.env.production`, so no override was needed there).

### 2026-05-18, backend-realtime, WS hardening

Choice: 5 boundary limits: an Origin allowlist on upgrade, a `maxPayload` frame cap, a per-IP token bucket on inbound messages, a per-IP connection cap, and a `bufferedAmount` close for slow consumers. The rate bucket and connection count are keyed by `CF-Connecting-IP`, shared per IP via `IpRegistry`.
Why: without these, one client can CSRF-connect from any origin, flood messages, open unbounded sessions, or stall the writer's memory on a slow socket.
Revisit when: the origin is firewalled to Cloudflare ranges (the open ROADMAP backlog item); until then `CF-Connecting-IP` is spoofable direct-to-origin and these per-IP limits are bypassable.

### 2026-05-18, infra-deploy, alpha topology

Choice: single OVH VPS-3 (12GB) with Coolify on the same host. Node heap is capped at 5GB and Mongo's cache at 1GB so services don't contend. `ws.*` is Cloudflare-proxied with an Origin CA cert (Full-strict TLS); the server pings every 30s since Cloudflare drops an idle WS at ~100s.
Why: a public WS edge needs Cloudflare's DDoS layer, which a DNS-only record doesn't get; an Origin CA cert is set-and-forget versus fighting Let's Encrypt HTTP-01 behind the proxy.
Revisit when: the origin isn't yet firewalled to Cloudflare ranges (`CF-Connecting-IP` is spoofable direct-to-origin) - the one item still open in the ROADMAP backlog.

### 2026-06-06, backend-realtime, spatial broadcast index and cluster-AABB scoping

Choice: drag/drop/cursor broadcasts are scoped through a `Map<cellKey, Set<Client>>` spatial index over the shared world grid (plus a global-subscriber fallback for viewports over a cell cap), instead of a linear scan of every client. Scoping uses the dragged cluster's full world AABB (stored per group, updated at merge), not just its origin point.
Why: linear scan was O(connected clients) per event, the scaling bottleneck; full-AABB scoping fixes large clusters being missed when their origin sat off-screen but their body didn't.

### 2026-06-06, backend-realtime, group spatial index and pan resync

Choice: a second per-process index (`GroupIndex`, one cell per group by body top-left) answers "which groups sit in these cells", so a client panning into a region gets resynced via `region_state`, closing the gap where a non-merging drop (scoped, unpersisted) never reached a client that wasn't watching. A resync applies only when the client isn't already holding or awaiting that group, so it never rewinds a newer live update.
Why: the broadcast index reaches current viewers of a live drop but does nothing for a later arrival; this index serves that read path the way the broadcast index serves the write path.

### 2026-05-21, backend-realtime, leaderboard scoring

Choice: each piece scores one point, credited to the user of the first `ClusterMerge` (by time) that lists it in `droppedPieceIds`, or in `lockedPieceIds` if no merge ever dragged it. Standings are maintained in memory (`LeaderboardTracker`: a scored-piece bitset plus a per-user tally), updated incrementally on every merge (`recordDrop`, anchoring or not, since a piece's first-ever drop can happen on either; an anchoring merge passes the whole set it locks, a superset of its dropped side). A full rebuild from the merge log (`MongoLogger.leaderboardScoreRows`, concatenating the same two arrays) seeds it at boot, reset, force-complete, and a slow defense-in-depth resync, the same split `MinimapGridTracker` already established for the minimap grid.
Why: a piece moves only when its group is dragged, so crediting the first drag gives each piece exactly one point; `droppedPieceIds` (not `addedPieceIds`) is the side the user actually dragged. The dragged side alone does not keep the standings whole, though: a piece that only ever sits on the receiving end (a cluster is dropped onto it where it lies, and that same drop anchors) locks with nobody credited for it, so the totals would end the event below the board's own piece count, which is the one number players check them against. Folding the locked set in at anchoring time closes that with no schema change, since a piece can only ever lock through an anchoring merge that lists it: every piece on a finished board has exactly one owner, and the standings sum to the full million. Re-running the full aggregation on every anchoring snap did show up in live latency: a 995 000-piece lock run's own rate degraded from 13 to 1.7 pieces/s as the merge log grew past ~600 000 documents, the aggregation's cost scaling with collection size. Moving that cost from per-event to a one-off rebuild mirrors the minimap grid's own fix for the identical shape of problem.
Revisit when: never expected; this now shares the minimap grid's already-proven-at-1M-scale pattern, including its resync cadence (see that entry's own revisit trigger).

### 2026-05-20, shared-protocol, lockedDelta stored on ClusterMerge

Choice: `ClusterMerge` persists `lockedDelta`, the pieces newly locked to the frame by that merge, even though other stats derive on demand.
Why: a frame anchor locks pieces without listing them in `addedPieceIds`, so the activity feed's "placed N pieces" count can't be reconstructed after the fact; it is a property of the event, not a precomputed counter.

### 2026-05-21, frontend-canvas, frustum culling and tiled zoom-out LOD

Choice: three techniques bound rendering to the visible window via one shared spatial index (`GroupGrid`): (1) per-frame frustum culling queries the index for the keep-ring around the viewport instead of scanning the board; (2) below `LOD_ENTER_ZOOM` (0.3) the play zone renders as baked render-texture tiles instead of live per-piece sprites, with hysteresis back above 0.35; tiles are LRU-capped by a VRAM budget with a screen-cover floor so a needed tile is never evicted.
Why: culling alone doesn't help fully zoomed out, where every piece is visible; the single play-zone texture it replaced lost sharpness as the board grew and cost O(board) to re-bake. Tiling makes per-piece density constant and bounds VRAM and bake cost to the visible window.
Revisit when: 1M runtime smoothness is verified (ROADMAP, closed). Standing limitation, by design: the active LOD band is overview-only, which a `lodActive` guard in `grabTargetAt` enforces rather than the tile visibility, since the clusters the band deliberately keeps live (held by a peer, or in a cell not yet baked) are hit-tested like any other. The three group layers also drop to `eventMode: "none"` while the band is up, purely so the affordance matches: Pixi reads its cursor off the hit target, which would otherwise still offer a grab hand over those live clusters. A press-drag or a sticky carry begun above the band still commits its drop inside it, so the rule is on starting a grab, not on where a cluster may land. If far-zoom grabbing is ever wanted, keep resting clusters hit-testable while hidden rather than lowering the threshold.

### 2026-05-21, frontend-canvas, play-zone hard limits

Choice: the board is bounded by a play zone computed once server-side (AABB of the frame and every scattered piece, widened by 50%, grid-snapped) and sent in `welcome`; camera and held pieces are clamped to it client-side, and `handleDrop` clamps the origin it persists to the same zone before anything reads it.
Why: a per-client computation would drift as pieces move inward while solving, giving late joiners a smaller zone than early ones; computing it once server-side from the seed keeps every client's bound identical. The server clamp is what makes the zone an invariant rather than a client convention: a drop is the only path that writes a position and the wire check on its coordinates is finiteness alone, so without it a forged drop could rest a cluster where no camera reaches and no viewport streams it, and far enough out that the world-grid cell key stops packing collision-free. It clamps rather than bounces because the client applies the same body-aware formula to its own input (with the tile margin on top, so its range is the narrower one), which makes the clamp a no-op on every honest drop.
Revisit when: a transient position ever becomes persistent. Drags are broadcast-only today, so the drop clamp covers every write.

### 2026-05-23, image-pipeline, reference image as a Deep Zoom pyramid

Choice: the slicer writes the reference image as a Deep Zoom pyramid (`source.dzi` + WebP tiles), opened directly by OpenSeadragon.
Why: one format covers both the alpha-scale and the gigapixel source; OSD fetches only the tiles the viewport needs, so a full-res image is never downloaded whole.
Revisit when: another consumer needs one full-resolution image (e.g. a thumbnail generator); serve that separately rather than re-adding a full AVIF to the manifest.

### 2026-05-22, infra-deploy, single R2 bucket for tiles and pieces

Choice: Deep Zoom tiles and per-piece AVIF textures share one R2 bucket (`mpp-assets`), separated by key prefix, fronted by a Cloudflare custom domain (not `r2.dev`).
Why: both asset sets share the same pipeline run, immutable lifecycle, and caching needs; a custom domain gives CDN cache control and no rate limit.
Revisit when: an asset set needs an independent cache TTL or access policy.

### 2026-05-23, image-pipeline, piece tiles bucketed by hundreds

Choice: piece tiles live at `pieces/<bucket>/<id>.avif`, `id` the seed-permuted wire id, `bucket = floor(id / 100)`. The manifest carries the resolved path; nothing reconstructs it from `id`.
Why: a flat million-entry directory is unwieldy for `ls`, R2 listings, and inspection; bucketing by 100 keeps both directory count and per-directory size comfortable at 1M scale.
Revisit when: never expected at this granularity; widen the padding if the puzzle grows past 1M.

### 2026-05-23, backend-realtime, manifest fetched from R2 at boot

Choice: the server fetches `manifest.json` from R2 once at boot and aborts on any network error, non-2xx, or id mismatch; no local fallback, no cache, no retry.
Why: R2, behind the CDN, is the single source of truth shared by server, frontend, and Pages build; fail-fast turns a bad manifest into a visible restart loop instead of a half-initialized process serving a stale puzzle.
Revisit when: a boot-time R2 read becomes a deploy hazard during an extended Cloudflare/R2 incident; keep a last-known-good manifest on a small persistent volume as a fallback then.

### 2026-05-28, qa-and-load, harness PASS criterion bounded to saturation signals

Choice: the load harness gates PASS on zero 1013 (backpressure) closes, zero `ws` errors, and under 5% server error frames. Latency (p50/p95/p99) is reported but not gated.
Why: the closed-alpha exit bar was "without server saturation"; an early 20-bot baseline showed multi-second latency bounded by the (since-fixed) serial dispatch and unthrottled drag broadcast, not by a load-induced fault, so an absolute latency gate would have conflated "does it stay up" with "is it snappy".

### 2026-06-07, qa-and-load, harness seeds sessions by direct Mongo write

Choice: the load harness seeds one disposable user and database session per bot directly in Mongo (Auth.js adapter shape, tagged `loadTest: true`), sending the matching session cookie on WS upgrade, instead of a dev-only session-minting endpoint.
Why: sign-in is Google OAuth only with no programmatic path, and a dev-gated mint endpoint would be live in prod (`MPP_DEV_ENABLED=1`), a standing way to mint sessions; direct seeding adds no new server attack surface.
Revisit when: the Auth.js adapter or session strategy changes (the seeded shape would drift), or a prod path with no Mongo tunnel is wanted.

### 2026-05-28, backend-realtime, scatter as a detached center-dense rounded-square band

Choice: the initial scatter samples each piece body in a rounded-square band detached from the frame (a gap, then a dense-mid-band halo), then derives the group origin from it.
Why: randomizing the origin alone left the solved image visible in place plus jitter; randomizing the body decouples the shuffled layout from the solved one, and the gap plus triangular radius make the cloud read as a detached, tipped-out pile rather than a band hugging the frame.
Revisit when: the gap or halo size, or the corner roundness, needs retuning by eye.

### 2026-06-01, auth-and-accounts, Auth.js via @auth/express with database sessions

Choice: Auth.js is mounted on the server's Express app, Google-only, with database sessions via `@auth/mongodb-adapter` sharing the one `MongoClient`. The cookie is `SameSite=Lax`, `httpOnly`, `Secure`-derived, domain-scoped for the `app.*`/`ws.*` split.
Why: `@auth/express` is the supported host, letting `ws` and auth share one process; database sessions give server-side revocation and a profile to hang the pseudo on.
Revisit when: a second writer instance is introduced, or sign-in needs more than Google.

### 2026-06-01, auth-and-accounts, WS upgrade requires a valid session

Choice: `verifyClient` resolves the session cookie via the adapter and rejects an upgrade with no valid session (401 at handshake); the connection carries the real `user._id`, replacing the old per-connection random id.
Why: contribution must be gated server-side, not just in the UI; reusing the SPA's own cookie needs no new protocol field.

### 2026-06-01, infra-deploy, auth secrets in Coolify env, not in the image

Choice: `AUTH_SECRET`/`AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` are set only in the Coolify service environment, never in the Dockerfile or repo; non-secret server config is baked as Dockerfile `ENV`.
Why: baking OAuth credentials into an image built and cached in CI leaks them; they belong in the orchestrator's secret store. A deployed compose file re-declaring the non-secret keys with localhost defaults previously shadowed the baked prod values (broken CORS, host-only cookie) until moved out.
Revisit when: a real secrets manager is introduced (all secrets move there together), or Coolify is reconfigured to merge the local override file (would reintroduce the shadowing).

### 2026-06-01, auth-and-accounts, login anti-abuse is internal per-IP rate limiting

Choice: two Redis-backed per-IP fixed-window counters (a generous window on `/auth` and `/profile`, a stricter one on the OAuth callback), fail-open on a Redis error. No Cloudflare Turnstile.
Why: with Google-only OAuth, each fake account already costs the attacker a real Google account; Turnstile's usual value (stopping password brute force) doesn't apply with no password form, so per-IP request-rate limiting is the remaining relevant surface.
Revisit when: IP rotation evades the per-IP budget under real public load; escalate to a Cloudflare edge Rate Limiting Rule on `/auth/*` then.

### 2026-06-03, frontend-canvas, viewport-driven texture streaming

Choice: per-piece textures and Pixi nodes are built on demand inside a ring around the viewport and freed on a wider hysteresis ring, instead of all at `build()`. `build()` now only creates lightweight containers and the spatial index from geometry, no textures fetched.
Why: fetching and building every piece at `build()` was O(board) in fetches, VRAM, and main-thread work; bounding to the visible window (~15-25k pieces at 1M) is the scaling move.
Revisit when: a background tab parks the ticker, so the initial fill pauses until focused. Resident VRAM at a deep zoom-out is addressed by [hot-tile residency](#2026-06-05-frontend-canvas-hot-tile-residency-frees-covered-cold-clusters).

### 2026-06-04, frontend-canvas, chunked board build with lazy geometry

Choice: `build()` runs in time-budgeted yielding passes (8ms bursts) instead of one synchronous block, and full per-piece edge geometry is generated only on hydration, cached and evicted on dehydrate.
Why: the eager path froze the main thread for seconds at 1M (geometry generation plus the group/index loop, all synchronous); only the visible window needs full geometry, so generating it for all 1M up front was wasted work.

### 2026-06-04, frontend-canvas, z-order layers replace world sortableChildren

Choice: stacking depth is now which fixed layer container (locked/unlocked/LOD-tiles/held) a group's container lives in, not a `zIndex` under `sortableChildren`.
Why: `sortableChildren` re-sorted the whole board on every grab/drop/anchor depth change; fixed layers make a depth change an O(1) reparent instead.
Revisit when: a grab at 1M hitches on the reparent's array splice out of a million-child layer; back the layers with a structure that removes in O(1) if so.

### 2026-06-05, frontend-canvas, hot-tile residency frees covered cold clusters

Choice: while the zoom-out LOD covers the board, a non-held cluster whose tiles are all baked and untouched for `LOD_HOT_TTL_MS` (9s) becomes evictable; a throttled per-frame sweep frees the coldest ones (LRU) once resident piece-node count exceeds `RESIDENT_PIECE_BUDGET` (24000).
Why: viewport-driven streaming kept the whole deep-zoom-out window resident behind the baked tiles, an unbounded VRAM cost; eager freeing on bake was tried and rejected (thrashes on any shallow zoom in/out). Budgeted LRU eviction only kicks in above budget, so the common case never evicts.
Revisit when: `RESIDENT_PIECE_BUDGET` is a node-count proxy for VRAM, derive from tile size if texture size varies; tune the hot TTL against observed play patterns.

### 2026-06-06, backend-realtime, viewport-scoped initial state on join

Choice: `welcome` carries no board; the client starts empty and streams groups in per viewport via `region_state` off the group index, bounded by viewport, not piece count. A default (unzoomed) viewport is a global subscriber and streams nothing; the overview covers the board until the user zooms in.
Why: a full `state` array on connect was O(board), ~25 MB at 1M, the last unscaled join path.
Revisit when: per-cell payload size bites (stream only changed groups).

### 2026-06-06, backend-realtime, server-computed minimap grid

Choice: the overview renders from a compact server-computed density grid (`MinimapGridTracker`, ~96 cells/axis), maintained incrementally on every drop/merge (moving only that group's own cells) rather than rescanned. A full recompute seeds it at boot/reset/force-complete, plus a slow (24h) defense-in-depth resync.
Why: the client can no longer scan the whole board for an overview under the viewport-scoped join; the grid used to be rebuilt from two full-board Redis pipelines every 5-minute keyframe tick, which starved concurrent gameplay for the whole live event, the one recurring O(board) cost this project otherwise eliminated everywhere else.
Revisit when: the 5-min broadcast cadence is too stale, the ~96-cell resolution reads too coarse at 1M, or the 24h resync interval (an unmeasured default) needs tuning against real traffic.

### 2026-06-08, frontend-shell, interested counter dedup by hashed-IP set

Choice: the landing "I'm interested" count is a Redis SET keyed per puzzle, one HMAC-SHA256-hashed visitor IP per member; the count is `SCARD`.
Why: a SET makes dedup inherent, with no separate counter to drift; hashing keeps a Redis dump from exposing raw IPs, since (unlike the short-lived rate-limit keys) this set is permanent.
Revisit when: set cardinality grows large enough to matter.

### 2026-06-08, infra-deploy, scheduled backups to a private R2 bucket

Choice: a `backup` sidecar dumps Mongo (`mongodump --archive`), Redis (RDB pulled over the wire), Umami's Postgres and Coolify's own Postgres plus its `/data/coolify` config tree every 6h to a private, separate R2 bucket (`mpp-backups`), keeping the newest 3 of each. rclone runs with `no_check_bucket`. Disabled locally by default.
Why: the single VPS has no off-host copy of live state otherwise; a private bucket is required since Mongo dumps carry user PII the public asset bucket can't host. Keep-3/6h is deliberately shallow: dumps are small and the dominant failure is total host or volume loss, which any recent backup covers. Coolify's config tree travels with its database because the `APP_KEY` that decrypts the database's stored secrets lives there, so neither half restores an instance alone. `no_check_bucket` is a hard requirement rather than tuning: a bucket-scoped R2 token holds no bucket-level rights, so rclone's pre-upload existence check is refused and the upload never leaves the host; widening the token to `Admin Read & Write` instead would let a sidecar that only writes objects to one bucket create and delete buckets across the account. Reaching Coolify's Postgres means joining the `coolify` network, where Coolify's own Redis holds the alias `redis` and wins that lookup over the game's, so the sidecar addresses the game's Redis by a `mpp-redis` alias only that service carries; the bare name is left in place for the server, which is on the app network alone.
Revisit when: point-in-time recovery is needed; today's snapshots are coarse recovery points, not continuous.

### 2026-06-09, piece-generation, edge uniqueness validated per-seed

Choice: "every shared edge unique" is validated per base seed by counting distinct 32-bit edge subseeds across the ~2M shared edges (`npm run validate:generation`), not guaranteed by entropy.
Why: a naive birthday estimate predicts collisions, but the subseed's multiplicative mixing is empirically a near-permutation on this grid lattice (0 collisions observed); counting is exact and cheap, and widening the seed would re-roll all geometry for a problem that doesn't occur.
Revisit when: widen the edge seed to 53 bits if the grid ever grows past 1000x1000.

### 2026-06-11, backend-realtime, per-tile piece cap on non-merging drops

Choice: a non-merging drop is rejected (bounced back with a `rollback`) when its destination world-grid cell already holds more than `MPP_TILE_PIECE_CAP_MULTIPLIER` (8) times the cell's solved density. Merges and frame anchors are exempt.
Why: the zoom-out LOD bakes a cell's live pieces into one texture; an unbounded pile on one tile would make that bake O(board).
Revisit when: 8x proves too loose or strict against live usage; note exempt merges can still push a cell over the cap and block an in-cell rearrange.

### 2026-06-11, frontend-shell, /play entry gate mirrors the scheduled start

Choice: a router guard redirects `/play` to landing while a real `eventStartsAt` is set and still in the future; an unset start or a failed landing fetch leaves `/play` open (fail-open).
Why: the gate should arm only when an operator schedules the event, so local dev and today's unscheduled prod stay reachable; fail-open avoids stranding a live visitor over a transient fetch blip, since this is a UX seal, not a security boundary (contribution is still gated server-side by the WS session).
Revisit when: a hard pre-launch lock is needed; move the decision server-side then.

### 2026-06-12, shared-protocol, anti-programmatic-solving via permuted wire ids and anchor offsets

Choice: ids are seed-permuted (`wireId = P(gridId)`, deterministic Fisher-Yates, built once at boot). A group's transmitted position is its anchor piece's world position; member pieces carry a grid-unit `(dx, dy)` offset from it, so no transmitted position exposes a solved-space coordinate. `generationSeed` lives server-only, as a Coolify secret.
Why: `id % gridCols` gave any script every piece's solved cell, and a public seed let it regenerate silhouettes to match tabs; either leak alone is enough to script-solve the puzzle, so both had to close together.
Revisit when: never expected; image-based (CV) solving stays explicitly out of scope.

### 2026-06-12, frontend-canvas, sticky carry mode (double-click or double-tap to stick a cluster to the cursor)

Choice: a double-press (double-click or double-tap) sticks a cluster to the cursor with no button held; a second double-press drops it, Escape returns it to pickup. It reuses the existing grab/drag/drop wire path; a 30s idle timeout (reset on real pointer moves) auto-drops it so a lock can't be parked indefinitely. The trigger is a hand-rolled detector (`consumeDoubleTap`: two pointerdowns within 350ms and 32 screen px of each other) driven off the same pointerdown that starts a press-drag, not the native `dblclick` DOM event.
Why: press-and-hold drag is tiring for long hauls and fights panning. The browser's own drag-vs-dblclick disambiguation was tried first to avoid a hand-rolled tap timer, but that made the gesture mouse-only in practice: `dblclick` fires unreliably off a touch double-tap. A manual detector on the pointerdown stream works identically for mouse, touch and pen and needed no new wire message, so it replaced the native event outright rather than adding a parallel touch-only path.
Revisit when: never expected at this project's scale; the 30s lock-hold stays an accepted standing limit, and the 350ms/32px thresholds are eyeballed, not measured against real touch use.

### 2026-08-06, frontend-canvas, pinch-to-zoom via document-level capture-phase listeners

Choice: two-finger pinch-to-zoom (and the same-gesture 2-finger pan) is driven by raw `PointerEvent`s bound on `document` with `capture: true`, entirely outside Pixi's own federated pointer events, rather than tracking `pointerId`/`pointerType` inside the existing `onStagePointerDown`/`onGroupPointerDown` handlers. A `this.pinch` guard at the top of both those handlers makes them no-ops while a pinch is active.
Why: Pixi's `EventSystem` binds its own native pointerdown listener directly on the canvas with `capture: true` (internal behavior, not part of its public API). A second listener added later on that same canvas element cannot run before it, whatever its own capture flag, since same-target listeners fire in registration order; only a listener on an ancestor (`document`, in the real capture phase) is guaranteed to resolve first. That ordering is what lets this class notice a second finger landing on a piece and set `this.pinch` before Pixi's own dispatch would otherwise route that event to the ordinary per-group grab and steal the first finger's held cluster.
Revisit when: a Pixi major version changes how `EventSystem` binds its native listeners (verified against pixi.js 8.18.1); re-check this ordering assumption on any Pixi upgrade.

### 2026-06-12, frontend-shell, activity feed event types (snap vs place, piece vs cluster)

Choice: the feed shows "place" (an anchoring merge, sized by `droppedSize`) and "snap" (a non-anchoring merge, sized by `mergedSize`) events; non-merging drops are never shown.
Why: snaps are already globally broadcast, so this costs two extra ints, not new traffic; drops are viewport-scoped and would need a new scalable signal to surface globally, not worth it for what would be mostly noise.
Revisit when: the snap over-report (e.g. "connected a 30-piece cluster" for adding one piece) feels unfair; switch to dragged-group size then.

### 2026-06-19, qa-and-load, soak-test state-corruption validator and per-bot IP spoofing

Choice: (1) `validate-state` reads Redis and Mongo at rest and asserts board invariants, the strongest being that replaying the `cluster_merges` log reconstructs the exact Redis partition. (2) The harness can send a distinct spoofed `CF-Connecting-IP` per bot to bypass the per-IP connection cap when load-testing straight against the origin.
Why: the harness verdict only covered transport saturation; state corruption needed a real invariant check, and log-replay equality is the same property the data model promises for the timelapse. IP spoofing rides the known unfirewalled-origin gap, the cheapest way past the cap for a load test we control.
Revisit when: the origin gets firewalled to Cloudflare ranges (the open ROADMAP backlog item): the spoof must move inside the edge, or raise the cap env instead.

### 2026-06-26, infra-deploy, admin ops page

Choice: a password-gated `GET /admin` (HTTP Basic, mounted only when `MPP_ADMIN_PASSWORD` is set) does three ops: clear everything, set the event start (applies live), and switch the puzzle (persists an override and restarts the process).
Why: puzzle id and seed are consumed at boot to build nearly all server state, so re-deriving them in-process would mean rewiring most of `main()`; persist-and-restart reuses the existing boot path instead of a fragile in-process re-init.
Revisit when: more than a couple of puzzles is wanted (move the list out of an env JSON); a hot switch with no restart is needed (pay the in-process re-init then).

### 2026-06-27, frontend-canvas, strict grab hit-test by sampling tile alpha

Choice: a grab only starts when the pointer is over opaque pixels, tested by sampling one texel of the tile's alpha via a reused 1x1 scratch canvas, rejecting Pixi's rectangular hit bounds (transparent tab margins, corners, gaps). A miss lets the press bubble to a pan instead.
Why: the alpha cut is the only silhouette the geometry-free client has, and reading it leaks nothing a bot couldn't already get from the rendered canvas; a per-click 1px read costs nothing, so no precomputed per-piece mask is needed.
Revisit when: rotation is enabled (the axis-aligned tile mapping breaks); touch/pen wants a fatter tolerance (sample a neighborhood, not one texel).

### 2026-06-27, frontend-canvas, the overview masks the server density grid under known regions

Choice: the overview skips painting the server density grid in cells the client already has live group knowledge of, leaving only the live dot overlay there.
Why: the server grid only refreshes on a snapshot tick, so it reads stale under a region the player is actively solving; masking keeps known regions live and the grid useful only where still needed.
Revisit when: masking granularity bites, since a coarse grid cell hides density for still-unknown pieces sharing it with a known group; subtract known counts from the grid instead of masking whole cells if that matters.

### 2026-06-30, auth-and-accounts, guest players are real users with a claim token

Choice: `POST /guest` mints a real `User` (`guest: true`) plus a database session through the same Auth.js adapter path a Google sign-in uses. A one-time claim token (only its hash persisted) lets a later Google sign-in absorb the guest via `POST /guest/claim`: an atomic anti-double-claim lock, `cluster_merges.userId` reattributed by `updateMany`, pseudo/country carried onto the target.
Why: open access wants immediate play with no OAuth round trip, but one identity model (a guest is a real `User`) keeps the WS gate, profile routes, and leaderboard unchanged; hashing the token mirrors the posture already used for session tokens.
Revisit when: the claim `updateMany` is O(merges) for the guest, fine per guest but revisit if one guest amasses a large board share. No inactivity sweep exists for throwaway guests; accepted, none planned.

### 2026-06-30, frontend-shell, guest claim fires on boot, gated by claimSettled

Choice: the guest claim token is stored client-side at mint and redeemed on app boot, not from a button, whenever it coexists with a fresh Google session. `/play` onboarding is held behind a `claimSettled` flag until the claim resolves, so a synced account never flashes a forced-pseudo modal.
Why: sign-in navigates away to Google and back, so there is no in-page callback to fire the claim from; keying it on "token plus Google session" makes it survive the redirect and self-correct a leftover token.
Revisit when: sign-in gains an in-page callback; move the trigger there instead of boot.

### 2026-07-01, backend-realtime, admission queue

Choice: a global cap on concurrent WS connections (`MPP_MAX_ACTIVE_CONNECTIONS`, default 0 = disabled) with an in-process FIFO wait list (`AdmissionController`). A client requests a ticket before connecting: under the cap it gets a one-time grant token immediately, over the cap it polls a queue position. A grant reserves a slot so the cap is never oversubscribed between grant and upgrade; TTLs reclaim abandoned grants and tickets.
Why: once everyone shares the one real-time path, the box needs a safety valve that sheds load into an orderly wait instead of accepting connections until it falls over.
Revisit when: fairness is per-ticket not per-user, so one user with many tabs holds many tickets; accepted at launch, revisit if queue-jumping by multi-tab becomes a real complaint.

### 2026-07-02, auth-and-accounts, 24h cooldown on pseudo/country changes

Choice: pseudo and country changes are throttled to once per 24h once already set; the very first assignment (guest mint or forced onboarding) is free. The check-then-write is two steps, not atomic.
Why: distinguishing "initial choice" from "change" by whether the field was already non-null keeps onboarding free while throttling repeat edits, with no separate onboarding flag needed.
Revisit when: the non-atomic check needs to be airtight (a single `findOneAndUpdate`); pseudo and country need different cooldowns.

### 2026-07-04, qa-and-load, load-test bot viewport memory bounded per window

Choice: `World.resetForNewViewport` clears the bot's local group/piece mirror on every viewport tick, keeping only a currently-held group, instead of accumulating every group ever seen.
Why: bots jump to a random viewport every second rather than panning; at 1M scale a dense random jump could pull in tens of thousands of groups per bot per second with nothing ever freed, OOM-ing the harness (~2GB heap, ~100s into a 50-bot run) at a scale smaller boards never hit.
Revisit when: the bot's movement model changes from random jump to smooth pan; move to ring-based eviction like the real frontend's instead of a full reset per tick.

### 2026-07-05, backend-realtime, grab reservation race and stale-hold sweep

Choice: (1) a grab reserves its group id in `client.held` synchronously at dispatch, before the Redis round trip, closing a window where a disconnect mid-grab could leave a group held by a gone connection with no release path. (2) A periodic sweep (30s) force-releases any group held longer than `MPP_STALE_HOLD_MS` (180000), covering the remaining case: a server crash or redeploy orphans holds since Redis survives restart but in-process `Client` state doesn't.
Why: an orphaned hold makes a piece permanently unsolvable (it can't be re-grabbed or merged into); the sweep gives that invariant a live enforcer instead of only a post-hoc validator.
Revisit when: `MPP_STALE_HOLD_MS` is an unmeasured heuristic ceiling; tune from observed live hold durations once the real event is running.

### 2026-07-10, auth-and-accounts, international opt-out reuses the UN flag code

Choice: the nationality picker offers a pinned "International" option alongside real countries, backed by `un`, the only non-ISO-3166-1 code `normalizeCountry` accepts. `un` is already a circle-flags asset (a globe glyph), so it needs no new SVG. The shared `COUNTRIES` list itself stays pure ISO codes; the picker renders "International" as its own labeled entry rather than looping it into the alphabetical list.
Why: reusing an existing flag asset skips a new SVG and upload step; accepting the code inside `normalizeCountry` means the server route, leaderboard bucketing, and topbar avatar all handle it like any other country with no special-casing.
Revisit when: never expected; a second opt-out-style code would need generalizing the single hardcoded check into a small allowlist.

### 2026-07-06, backend-realtime, paced region_state batching

Choice: a viewport's `region_state` resync for newly entered cells is split into several paced messages, batched by non-overlapping world-grid columns (`MPP_REGION_STREAM_BATCH_CELLS` cells each), sent with a poll-and-wait between batches until `ws.bufferedAmount` clears half the hard limit. A new `viewport` on the same connection supersedes an in-flight stream.
Why: a big viewport jump (overview jump, fast zoom) could enter up to 256 cells at once, sending close enough to the 4MB buffered-amount limit to trip the slow-consumer close; column-range batches keep `coverage` provably correct per batch.
Revisit when: `MPP_REGION_STREAM_BATCH_CELLS`/`_POLL_INTERVAL_MS` held clean through a 2026-07-06 re-soak (50 bots) but real public traffic could still exceed that; tighten if 1013 closes reappear. Raising the buffered-amount limit itself is a separate, deferred follow-up.

### 2026-07-11, frontend-canvas, overview loose/locked layers scale independently

Choice: the density-grid overview paints the loose and locked layers each against their own per-layer max cell count, not one shared max.
Why: loose pieces vastly outnumber locked ones for most of the puzzle's life (nearly all 1M start loose), so a shared max diluted locked cells to near-background alpha right when "which pieces are placed" is the signal a player most wants to read; verified live on the dev board (35 locked of 1M), a locked cell rendered at alpha 0.2 (indistinguishable from the loose tint) under the shared max versus 0.75 under the independent one.
Revisit when: never expected; a lone locked cell now reads at near-full alpha regardless of how sparse it is relative to the rest of the board, the intended trade-off (progress must stay legible over calibrated density), mirroring how the loose layer already scales against its own max.

### 2026-07-23, frontend-canvas, texture hydration queue reordered by viewport proximity

Choice: `pumpHydration` reorders the pending `hydrateQueue` instead of draining strict FIFO, but only when the queue holds more than the in-flight cap can start this frame; a queue that fits within capacity starts regardless of order, so the reorder pass is skipped in the common case. The order is a partition, every group intersecting the viewport ahead of every group that only reaches into the hydrate margin, with squared distance to the viewport center (nearest popped first) as the tie-break inside each block.
Why: entries were queued in discovery order during `reconcileGroups`, which tracks the path a pan swept, not where it stops. A fast or long pan built a backlog of groups from along that path ahead of the groups under the just-settled viewport, so a player landing somewhere new waited on stale, no-longer-relevant fetches to drain first. The partition is what keeps the fill front rectangular: distance ranks against a point while the viewport is a rectangle, so a group sitting just off-screen on a center axis is nearer the center than one in the viewport's own corner, and the screen fills as a disc whose sides and corners wait behind pieces the player cannot see. The reorder is a full array sort each time it runs (not a proper priority heap); acceptable because it is gated on an actual backlog and the queue is bounded by groups near the viewport, not board size.
Revisit when: a soak shows the sort itself costing frame time, i.e. backlogs routinely far larger than what the hydrate ring's margin should ever hold; a binary heap would drop the per-pump cost from O(n log n) to O(log n) per pop.

### 2026-07-23, backend-realtime, locked pieces stop being a group

Choice: on anchor, a cluster's group is deleted rather than merged into a growing locked group; `locked` moves to a piece-level flag (`piece:<id>.locked`), and a locked piece's position is never stored, since it is implicitly its own canonical solved position at internal origin (0,0). `detectSnap` treats any locked grid-neighbor as an automatic match against that implicit target, still gated by the drop's own tolerance to it (a locked neighbor never substitutes for that check). `MPP_CLUSTER_PIECE_CAP` (default 20000) caps a loose-loose merge only; an anchoring merge is exempt, since it dissolves rather than grows a group.
Why: the old model kept folding every newly-anchored cluster into one ever-growing locked group; near completion that produced a single group of hundreds of thousands of members, and the client's hydration path fetched a whole group's members in one unbounded batch on a resync, an unbounded-load crash risk.

### 2026-07-23, backend-realtime, locked-piece delivery via a bitset, not a reverse index

Choice: `LockedPieceIndex` is a flat `Uint8Array` of one byte per piece (is-it-locked), with a cell's candidate piece ids recomputed on read from the grid geometry (`candidateGridIdsForCell`), not a stored `cell -> pieceIds` map.
Why: unlike a group's arbitrary live drop position (why `GroupIndex` needs a stored reverse map), a locked piece's cell is a pure function of its own grid id, fixed at generation and never changing once locked; `minimap.ts`'s density grid already treats a locked piece's cell the same way (`cellIndexForPiece(p.id, 0, 0, ...)`, computed, never looked up). A `Map<cellKey, Set<pieceId>>` at up to ~1M entries costs far more memory for no lookup-speed win at this scale.
Revisit when: never expected; the geometry this relies on (grid id -> canonical (col, row)) is fixed at generation.

### 2026-07-23, frontend-canvas, locked pieces render flat, decoupled from GroupNode

Choice: a locked piece is a slot in a flat `lockedPieces` map (`constructLockedPiece`) holding its world bounds and its spatial-index entry, keyed in the same numeric space as group ids and never routed through the group-keyed bookkeeping. The slot's `node` stays null and no texture is ever fetched for it; only an anchoring snap fills one, salvaging an already-hydrated member straight out of its dying `GroupNode` (reparented, no re-fetch).
Why: the bounds and the index entry are what the overview overlay and the salvage bridge read, and they cost nothing to hold; a piece the player just dragged already has its texture loaded, so refetching it the instant it locks would be wasted network and a visible flicker. A locked piece has no membership, merge, or hold/drag, so forcing it through the group path would add branches to every one of those for a case that never applies to it.
Revisit when: never expected; `DziRevealLayer` is the only rendering path for locked content, so the only nodes this map ever holds are salvaged ones, on the lifetime and eviction rules the salvage entry below sets.

### 2026-07-24, backend-realtime, per-cell compositing runs on a debounced dirty-cell queue

Choice: `applyMerge`'s anchor path only flags a cell's key dirty (`CellCompositor.markDirty`); a single background loop drains dirty cells one at a time, composites, and uploads, so a burst of lock events touching the same cell (a busy area filling in, or the dev force-complete shortcut) collapses into one rebake per cell instead of one per event. `forceComplete` has no per-piece incremental hook (it anchors directly via `anchorAllGroups`), so it dirties every cell in the grid at once rather than computing which ones actually changed; a rare dev-only bulk op, so redundantly recompositing an already-finished cell is an accepted one-off cost. A failed bake is retried with bounded attempts before being logged and dropped for good; see bounded per-cell bake retries below.
Why: synchronous compositing inline in the lock-handling path would redo the same cell's bake repeatedly under exactly the traffic patterns (a filling-in hotspot, force-complete, the 995k-lock seed scenario) this project has already hit and fixed elsewhere (per-group dispatch queues, incremental minimap grid, paced region_state); a debounced queue keeps the same protection with no new process or IPC.
Revisit when: never expected. The one-at-a-time drain kept up against real bake volume at the ~995 000-piece prod scale (2026-08-03, see bounded per-cell bake retries); bulk-seeding traffic instead gets its own end-of-run batching (see seed-lock-scenario defers compositing) rather than needing concurrent draining here.

### 2026-07-24, backend-realtime, cell composite geometry mirrors individual piece-tile bleed

Choice: a cell's composite canvas is `WORLD_TILE_SIZE + 2*margin` square (not a bare `WORLD_TILE_SIZE`), and its input piece set is not just the pieces the cell strictly owns but every piece within one column/row of that ownership range (`haloGridIdsForCell`), cropped to the canvas bounds before compositing (sharp's `composite()` throws rather than clipping an oversized or offscreen input). `cellKeysForGridId`/`allCellKeysForGrid` still use the plain (non-halo) cell definition for dirty-marking and enumeration.
Why: an individual piece's own tile already bleeds `margin` px past its canonical cell (so its tab can bulge into a neighbor), and margin is always < pieceSize (see tile margin), so a piece up to one column/row outside a cell's strict ownership can still have its tab reach that cell's canvas. Widening the canvas and the input set the same amount reproduces exactly the overlap individual piece tiles already have with each other; omitting it would visibly clip a tab at every cell boundary.
Revisit when: never expected; the margin-vs-pieceSize relationship this relies on is fixed at generation (see tile margin).

### 2026-07-24, backend-realtime, cell composites are version-suffixed immutable R2 objects, first live server write to R2

Choice: each rebake uploads to `<puzzleId>/cells/<cellKey>/<version>.avif` in the existing public `mpp-assets` bucket (a new key per version, never overwritten in place); `SRegionState.cellComposites` and the `cell_composite` broadcast carry `{cellKey, version}`, and the frontend derives the fetch URL from those two numbers, needing no manifest indirection (a cell position is not a solved-adjacency secret). Once a rebake's new version is fully live (index, Redis, broadcast), the version it superseded is deleted, so storage stays bounded by cell count instead of growing with every lock event over the puzzle's whole lifetime. Reads (each locked piece's own tile, to composite from) go over the same plain public HTTPS the frontend already uses; only the upload and the delete need credentials, `MPP_R2_ENDPOINT`/`MPP_R2_ACCESS_KEY_ID`/`MPP_R2_SECRET_ACCESS_KEY` (new `@aws-sdk/client-s3` dependency, R2's S3-compatible API), set in the Coolify env. All three unset (local dev's default) leaves `ctx.cellCompositor` unconstructed and compositing fully inert rather than failing the boot. A board reset (`PuzzleLifecycle.resetCurrent`) additionally bulk-deletes the whole `<puzzleId>/cells/` prefix (`CellCompositor.clearAll`, `R2Client.removeByPrefix`, paginated `ListObjectsV2` + `DeleteObjects`) rather than the last-known version per cell, since the reset empties the version index that a per-key delete would need to target, and a per-key delete would still miss any object a still-earlier reset already orphaned before this existed.
Why: an immutable-while-live, version-suffixed key matches the caching model every other R2 asset here already has (upload once, cache forever), needing no new Cloudflare cache rule or invalidation; every other R2 interaction before this was either a boot-time manifest read or the offline `rclone`-based pipeline upload, never a write from the live process. Deleting only the immediate predecessor, and only after the new version is confirmed live everywhere a reader could find it, keeps that guarantee for every version a client could actually be pointed at, while still bounding total storage. The reset's own cleanup has to go by prefix rather than by key for the same reason: a reset's whole point is discarding the version bookkeeping, so nothing is left to compute a specific key from.
Revisit when: never expected. The reset's bulk delete is best-effort (logged and dropped on failure, same as the per-rebake predecessor delete) and does not block the reset itself.

### 2026-07-26, frontend-canvas, salvage is the only bridge left, exempt from budget eviction

Choice: a piece anchored from the local client's own drag is still salvaged straight out of its dying `GroupNode` (reusing its already-hydrated texture, zero fetch); every other newly-locked piece (unhydrated, or no known owner) gets no individual rendering at all until `DziRevealLayer` reveals it. A salvaged node is freed once it leaves the wide keep ring (off-screen, so freeing it there is invisible); `DziRevealLayer` tracks coverage per cell, not per piece, so there is no earlier "the reveal layer already shows this piece" signal to free it on sooner. It is never subject to `evictResidentsOverBudget`: unlike a group, there is no re-fetch to recover a salvaged node freed early, so eviction pressure must never reach it.
Why: this is the one gap-closing exception the retired per-piece fallback used to cover for free, now scoped to the rare, self-limiting salvage population instead of every locked piece. Freeing only on ring-exit, rather than replicating a precise per-piece coverage snapshot against `DziRevealLayer`'s cell-level hydration, trades a few extra seconds of an already-correct salvaged sprite staying resident on screen for not adding new tracking to a rendering path that has already seen several real-scale correctness bugs (see the DZI reveal entry above).
Revisit when: never expected; salvaged pieces are bounded by this client's own recent anchors and self-clear once they scroll off-screen.

### 2026-07-26, backend-realtime, no compositor configured means locked pieces do not render

Choice: an environment with no R2 write credentials (`config.r2Write` unset, local dev's default) never composites any cell; with the server-composited reveal the only rendering path for locked content, locked pieces in such an environment render as nothing rather than degrading to individual fetches.
Why: one rendering path was the explicit instruction, removed rather than kept as a second path; local dev without R2 credentials already showed zero server-composited tiles, so this is a widening of an already-accepted local-only gap, not a new category of failure. Confirmed with the user before implementing.
Revisit when: never expected; set the R2 write credentials to see locked content in any environment, the same requirement compositing itself already had.

### 2026-07-27, qa-and-load, seed-lock-scenario locks pieces through the real grab/drop path

Choice: `seed-lock-scenario.ts` locks its chosen pieces by calling the real `handleGrab`/`handleDrop` (and so `applyMerge`) in-process, dropping each singleton at internal origin `(0, 0)`, instead of writing `piece:<id>.locked` and deleting the group directly in Redis. Origin `(0, 0)` is `handleDrop`'s `frameAnchor` path (see handlers.ts) because a piece's local AABB already bakes in its solved grid cell (`col * pieceSize, row * pieceSize`, see worldGrid.ts); origin is a pure translation on top of that, not the piece's own world position (an earlier version of this script passed the world position directly, which silently anchored nothing: `frameAnchor` was false for every piece off the very first cell, so every "lock" was actually just an ordinary unlocked move with no error). The script owns its own in-process Hub (no clients ever added, so every broadcast/send is a no-op) / GroupIndex / LockedPieceIndex / CellCompositor, and waits on `CellCompositor.whenIdle()` before exiting, mirroring `index.ts`'s own wiring so every touched cell bakes exactly as live play would.
Why: the direct-write shortcut never touched `CellCompositor.markDirty`, the one thing that populates the composites; the board was left with zero composites until a now-deleted backfill script (`backfill-cell-composite-level0.ts`) patched that gap after the fact, and it still left a handful of cells permanently unbaked after retries. Driving the real handler path costs one real Redis/Mongo round trip per piece instead of one pipelined batch write, but produces the exact same state a live lock event would, with no separate backfill step and no possibility of the seeded board and the compositor's dirty-tracking disagreeing.
Revisit when: never expected. Verified on prod's `synthetic-1m` at 10 000-piece scale (2026-07-27): 10 051/10 051 pieces locked, all 1 274/1 274 touched cells composited, no gaps. Re-verified at 100 000-piece scale (2026-07-29): 99 614/99 614 pieces locked, `validate-state` passes every invariant, full 1 296/1 296-cell grid composited. Verified at the real ~995 000-piece scale (2026-08-03, compositing deferred to one end-of-run pass, see below): 994 977/1 000 000 locked, all 1 296 cells composited clean across two full-grid straggler audits, with only 2 cells ever exhausting the bounded bake-retry ceiling on a transient R2 fault (see bounded per-cell bake retries). Client-side rendering confirmed visually in a real browser at every scale (the automated-preview limitation noted in the DZI-reveal entry below still blocks a scripted pass).

### 2026-07-28, frontend-canvas, per-tile loading badge removed

Choice: `LoadingOverlay` (the pulsing dark-scrim, cream-border badge drawn over a viewport cell whose content is known but not yet displayed) is deleted outright rather than hidden. `computeLoadingCells` stays: it still gates the initial-fill loading cover (every viewport tile must be baked before that cover drops), only the per-cell visual drawn on top of it is gone.
Why: flagged directly against a live screenshot as visually unpleasant ("gray squares") during ordinary play; the badge fired routinely during normal panning and zooming, not just the rare cold-start case it was meant to soften.
Revisit when: a cell whose content has not streamed in yet is now visually identical to an empty one (just the plain backdrop) while zoomed in; if that reads as a bug (missing pieces) rather than "still loading" during a fast pan into a fresh region, a lighter affordance may be worth reintroducing.

### 2026-07-28, frontend-canvas, DZI reveal mask and seam baked server-side via CellCompositor

Choice: locked content renders through `DziRevealLayer`, the only rendering path for locked pieces. `CellCompositor.processCell` rasterizes a silhouette mask and a seam (border-only) texture per cell, straight from `generatePieceGeometry` (no piece-tile fetch needed), at 3 LOD tiers (`CELL_MASK_TIER_FACTORS`) to bound VRAM at real board scale, sharing the existing `{cellKey, version}` R2/versioning model (`region_state`/`cell_composite`, no `PROTOCOL_VERSION` bump). Client-side, every hydrated per-cell mask sprite combines into one `RenderTexture`, and one Sprite of it masks `tileContainer` (see the viewport-clipped `MaskFilter` entry): Pixi only respects real per-pixel alpha for a single Sprite mask, not a `Container` of sprites (which silently falls back to `StencilMask`, so every locked cell reveals as a flat rectangle instead of a silhouette). The seam texture draws unmasked on top.
Why: reuses a pipeline and R2/versioning model already proven in prod instead of building a second one, and needed no wire change; chosen over sending raw per-piece Bezier parameters to the client, a materially bigger anti-programmatic-solving exposure than a raster image (see anti-programmatic-solving). Verified pixel-precise and fast at real board scale (994,977 / 1,000,000 locked) across several real-browser passes, which fixed in turn: VRAM thrashing (LOD tiering, corrected tier breakpoints), pan lag (an unconditional combined-mask rebake throttle), incomplete on-screen coverage (the mask sprite needs a camera-transformed parent), a soft-photo regression (the DZI pyramid's WebP quality decoupled from the AVIF tiles' own), and a family of WebGL failures at high zoom, all rooted in how large Pixi sizes the mask pass (see the viewport-clipped `MaskFilter` entry): `antialias: true` dropped from the main canvas and every render-to-texture bake in this codebase, and the masked container's bounds pinned to the viewport ring via `Container.boundsArea`, set on the effects-free `baseTileContainer` child rather than the masked `tileContainer` itself (a real Pixi gotcha: `boundsArea` on a container that also carries a mask effect silently corrupts that container's own computed bounds).
Revisit when: `DZI_QUALITY` (85 in `slice-image.ts`, decoupled from AVIF's `--quality`) is an eyeballed AVIF-60-equivalent estimate, not a measured match. The trigger has fired and the look is outstanding: prod serves the real `earth-mosaic` photo since 2026-08-15, sliced at that value, and nobody has judged it by eye since. A retune is `slice-image.ts --dzi-only` plus a re-upload of the pyramid, leaving the pieces and the manifest untouched.

### 2026-07-29, image-pipeline, tile licensing is CC0/public-domain only, no attribution UI

Choice: `fetch-tile-images.ts` only accepts a Wikimedia Commons file whose `extmetadata.LicenseShortName` is in an allowlist (CC0, CC0 1.0, Public domain, Public Domain Mark 1.0); every other license, including attribution-required ones like the common CC BY/CC BY-SA, is rejected regardless of color match quality.
Why: the app has no photo-credit UI anywhere and none is planned, so a license requiring attribution can't be honored; CC0/public-domain needs none. Live testing during planning showed this is a small minority of Commons nature/animal photography, so the fetcher paginates deep per category and prints an accepted/scanned tally to reveal when `--categories` needs widening.
Revisit when: never expected; the manifest keeps each tile's license and description URL for provenance/audit even though nothing renders it.

### 2026-07-29, image-pipeline, mosaic tiles are irregular bordered regions, not a uniform square grid

Choice: `build-mosaic.ts` splits the piece grid into horizontal bands of random height in `[--min-block, --max-block]` (default 3-6 pieces), each independently split into column slices of random width in the same range, so seams between bands don't line up (a running-bond, brick-like offset) and one tile photo can land on a wide, tall, or square region. A `--border`-px `--border-color` seam (default 1px, near-black, dropped from an initial 3px per user feedback so it stays a hairline even on the individual pieces `slice-image.ts` later cuts at a region's edge) is inset from every region's own edges before the tinted, blended stamp fills the rest. Each stamp is tinted toward its region's target color via sharp's `.tint()` (a Lab-space recolor that preserves the source photo's own luminance and texture) blended against the untouched original by `--blend` (default 0.7).
Why: a uniform grid of same-size square stamps read as too mechanical; independent per-band splits plus a grout-line border give the deliberate variety and chaos a real photo mosaic has, closer to physical mosaic tiling than a spreadsheet of thumbnails. Every region still resolves to whole pieces (`--min-block`/`--max-block` are in piece units), so `slice-image.ts` downstream sees no difference from a uniform grid.
Revisit when: never expected; `--min-block`/`--max-block` equal to each other, and `--border 0`, both still fall back to the old uniform look for a from-scratch retune by eye.

### 2026-07-29, image-pipeline, main image resize is a non-cropping stretch, no grid aspect-ratio constraint

Choice: `build-mosaic.ts` samples the main image once per piece (`{fit: "fill"}`, stretched to the full `--rows`x`--cols` grid, no crop) rather than `slice-image.ts`'s own center-crop; `--rows`/`--cols` carry no divisibility constraint of any kind (the irregular-region partition tiles any total exactly).
Why: cropping would permanently discard part of a one-of-a-kind source photo (the NASA Blue Marble image) for a puzzle whose whole point is showing it; `fill` keeps every pixel, at the cost of distortion if `--rows`/`--cols` isn't chosen proportional to the source's own aspect ratio, which is left to whoever runs the real production build rather than auto-derived from the image.
Revisit when: never expected; look at the real output once a real `--main` and a matching `--rows`/`--cols` are picked.

### 2026-07-29, image-pipeline, mosaic builder inherits the 72px/piece gigapixel ceiling on Windows, real runs go through Docker

Choice: `build-mosaic.ts --piece-size` still defaults to 72 for a quick native-Windows smoke test, but any real production run goes through this repo's Docker (Linux) instead, where the identical sharp/libvips version writes 14+ gigapixel images with no ceiling encountered (verified at both 72000x72000 and 120000x120000 blank writes, and end to end at 120px/piece for the real 1000x1000 grid).
Why: the "~6 gigapixel" Windows ceiling once recorded here predated a libvips upgrade and turned out stale; the real one sits far lower (a blank write segfaults around 71000x71000px, ~5.0 GP) and isn't even a clean pixel-count boundary, a many-real-file composite can fail below that same threshold too. Chasing the exact Windows boundary further wasn't worth it once Linux was confirmed clean.
Revisit when: never expected; Docker is the answer for any real-scale run on this machine.

### 2026-07-30, image-pipeline, tile assignment never repeats a photo, fails fast if short

Choice: `selectTiles` now assigns each region a tile that has never been used anywhere else in the mosaic (removed the old `--candidates`/`--min-reuse-distance` local-avoidance entirely, a hard global constraint instead). `main()` checks `regions.length <= tileFiles.length` up front, before any per-tile color decode or main-image sampling, and fails with the exact shortfall and a `fetch-tile-images.ts` re-run suggestion if short.
Why: requested explicitly, no photo anywhere in the finished puzzle should repeat; failing fast with the exact number needed turns "not enough tiles" into one clear message instead of a silent quality drop or a crash partway through the expensive render pass.
Revisit when: tested against the real main image (a 4096x4096 Earth+Moon render) at a 110x110 scale (616 regions, comfortably inside the 750-tile library): avg dE76 rose to ~31 (versus ~9-22 with reuse allowed) and the result does not read as Earth at all. The library is ~83% "Trees"-category tiles (dark green/gray/brown) with almost no deep-blue or near-black ones to cover the ocean and space that dominate the source image; once those few dark tiles are claimed, every later region needing that color is forced into a poor match. The real full-scale grid needs roughly 50 000 distinct tiles at default settings, so before fetching anywhere near that many, `--categories` needs deliberately color-diverse additions (ocean/sea, night sky, astrophotography) or the mismatch gets worse at scale, not better.

### 2026-07-30, image-pipeline, optional global tint blends every chunk toward a crop of the main image itself

Choice: `--global-tint` (0-1, default 0 = off) blends each rendered chunk's finished buffer against a same-region crop of `--main` (resized once to its own native resolution and cached, so every chunk crops cheaply from that one buffer instead of re-reading the source file). Unlike the per-region `.tint()`, this is a second, spatially-varying pass over the whole assembled image, nudging every pixel toward the real source photo's own local color rather than one flat color per stamp.
Why: proposed as a cheap way to make the mosaic read as its main image even where the tile library's color palette can't locally match well (see the entry above); confirmed at the same 110x110 test scale against the real Earth+Moon image: 0.2 ("very light", as first requested) is barely perceptible and does not fix the readability problem; 0.5 makes the globe, continents and Moon clearly recognizable while individual tile photos are still visible underneath. No fixed default is set since this is a legitimate aesthetic call between "reads as the source image" and "reads as a mosaic of individual photos"; left to the caller to pick.
Revisit when: never expected; retune by eye once the real full-scale tile library exists, since color-match quality (and so how much correction is needed) will differ from this small test.

### 2026-07-31, qa-and-load, seed-lock-scenario defers compositing to one end-of-run pass

Choice: the bulk locking loop no longer attaches `cellCompositor` to `ctx` while it runs (so `applyMerge`'s per-lock `markDirty` call is a no-op there, the same as a deployment with no R2 write credentials). Every touched cell is collected instead, then marked dirty and drained in a single pass after every piece has locked, with a bounded retry (re-mark whatever still has no composite, up to `COMPOSITE_RETRY_ATTEMPTS`) standing in for the later-lock self-heal live play gets for free.
Why: `CellCompositor.drain` processes dirty cells strictly one at a time with no coalescing once a cell finishes baking, so firing `markDirty` per lock at bulk-seed scale rebakes the same ~1 296 cells from scratch many times over instead of once, the exact throughput drop the 100 000-piece run's own revisit-later note flagged (see the seed-lock-scenario entry above).
Revisit when: never expected. Verified via the same 2026-08-03 real ~995 000-piece run (see seed-lock-scenario entry above): the deferred, single end-of-run compositing pass kept up, with the leaderboard fix above closing the other bottleneck that had been blocking that same run.

### 2026-08-03, backend-realtime, bounded per-cell bake retries

Choice: `CellCompositor.drain`'s catch block requeues a failed cell (re-added to `dirty`) up to `CELL_BAKE_MAX_ATTEMPTS` (3) total attempts, tracked per key in `bakeAttempts` and cleared on success or on giving up, before logging and dropping it for good. The requeue needs no timer: it lands behind whatever else is already in the queue, which is itself a natural backoff, and costs nothing when nothing is failing.
Why: the original debounced-queue design's "logged and dropped, not retried" choice (see per-cell compositing above) rested on two premises that don't hold near the end of a puzzle's life: a later lock event would re-dirty the cell and self-heal it (false once a cell is at or near 100% locked, with few or no lock events left to trigger that), and a permanently-missing composite only cost a rendering optimization the client already fell back from (false since the server-composited reveal became the only rendering path, see the DZI reveal entry above). The retry is a bounded ceiling layered on top of that same self-heal, not a replacement for it.
Revisit when: never expected. Verified 2026-08-03 against the real ~99.5%-locked (994 977/1 000 000) synthetic-1m prod board across two full-grid straggler audits (464 cells total, from causes since resolved: an unbounded piece-tile-fetch overwhelming the CDN during compositing, since removed along with the photo tier itself, and a pre-mask/seam-tiering legacy bake format); all but 2 recovered clean on a targeted resweep. Those 2 exhausted the retry ceiling on a genuine transient R2 fault (a specific object serving a persistent 503 on GET despite a correct HEAD response, cleared by a forced re-upload of the same bytes) and gave up with the exact failing URL logged instead of hanging or silently corrupting state, the one real exercise of this safety net so far.

### 2026-08-03, backend-realtime, admin resweep accepts an explicit cell-key list

Choice: `POST /admin/resweep-composites` accepts an optional `cellKeys: number[]` in its JSON body; when present, only those cells are marked dirty instead of the whole grid (`allCellKeysForGrid`), with everything else about the endpoint (fire-and-forget, `/status` polling, `NoCompositorError` handling) unchanged. The admin page's own button still always sends `{}` (full grid); the targeted form is curl/scripted-only, no UI added for it.
Why: a full-grid resweep re-bakes every cell whether it needs it or not, so sweeping all 1296 cells to fix a handful of genuine stragglers costs redundant R2 work and background CPU load on the same box serving live players. A real ~99.5%-locked prod audit found exactly this shape of problem: only 10 of 1296 cells actually lacked a composite (confirmed against both the Redis version index and a direct R2 bucket listing, not just the endpoint's own pending count), so targeting those 10 directly finishes in the time a couple of cells take instead of the whole grid.
Revisit when: verified 2026-08-03, twice, at the real 10-cell and a later 454-cell straggler set (see bounded per-cell bake retries above): both drained fully in the background well inside a full-grid sweep's own time (hours, not the full sweep's estimated 10+), with no attention needed. Those runs measured a per-cell bake that also fetched piece tiles and composited a photo tier, both since removed; a resweep is faster now, so the gap over a full-grid sweep is at least as wide today. Never expected beyond that; if straggler audits become routine rather than a rare post-incident cleanup, consider exposing this from the admin page's own UI instead of curl-only.

### 2026-08-05, backend-realtime, retire the server-side full-photo composite tier

Choice: `CellCompositor` no longer fetches piece tiles or bakes/uploads a photo AVIF alongside the mask/seam tiers; `processCell` is now pure in-memory work (geometry -> SVG -> AVIF for mask/seam only), with no network fetch of any kind, and `finishBake`/`objectKey` drop the photo upload and its stale-version cleanup.
Why: `DziRevealLayer` (see the DZI reveal entry above) is the only locked-piece rendering path and never fetches the bare `<version>.avif` URL, only the `-mask-{tier}`/`-seam-{tier}` ones, so the photo tier was baked and stored for no reader; dropping the fetch also means a cell bake no longer touches the CDN at all, only in-process work plus an R2 upload.
Revisit when: never expected. Deployed to prod 2026-08-05 (main fast-forwarded to this commit, confirmed via the running container's image tag matching the commit SHA and a clean boot log), then the orphaned photo objects already on R2 were bulk-deleted by rclone filter (2560 bare `<version>.avif` objects, ~794 MiB, isolated from live mask/seam objects by the absence of any `-mask`/`-seam` suffix), along with a separate, unrelated 2580 objects (~728 MiB) left over from before mask/seam LOD tiering existed (`<version>-mask.avif`/`<version>-seam.avif`, no tier number) that nothing had read since tiering shipped either. `synthetic-1m/cells/` dropped from 2.46 GiB to 997 MiB, matching the live tiered mask/seam set exactly, confirmed by re-listing both deleted patterns at zero afterward.

### 2026-08-05, backend-realtime, pipeline piece-group reassignment on loose-loose merges

Choice: `applyMerge`'s non-anchoring branch reassigns every merged-away piece's `groupId` via one pipelined `RedisState.setPieceGroups` call (mirrors `lockPieces`), instead of one `setPieceGroup` round trip per piece.
Why: every other write in this function already pipelines (`lockPieces`, `deleteGroup`, `releaseGroup`); this was the one path still paying one Redis round trip per piece, up to `MPP_CLUSTER_PIECE_CAP` (20000) pieces on a single merge.
Revisit when: never expected functionally, but this path is still unverified at real merge volume: `seed-lock-scenario.ts` only ever drops singletons at the frame origin (always the frame-anchor path), so no soak run has exercised a large loose-loose merge (two big unlocked clusters joining just under the cap).

### 2026-08-06, frontend-shell, the HUD keeps only the reference thumbnail and the overview on a small screen

Choice: a viewport at most 680px wide or 480px tall is compact (`useCompactViewport`, one module-level `matchMedia` shared by every reader, mirrored by the same query wherever CSS needs it). There `ContributorsPanel`, `ActivityPanel`, `ZoomControls` and `FlagBar` are not rendered at all and the options menu's Display section does not list them, both answering `isPanelAvailable`, so a switch stored on a desktop cannot bring one back onto a screen with no room for it. What stays is `ReferencePanel` top-left and `OverviewPanel` bottom-right at `min(152px, calc(50vw - 20px), <height budget>)` with 10px edge insets; `TopBar` keeps its own responsive pass (flanking grid columns to `minmax(0, 1fr)` with ellipsis on the wordmark and pseudo, a 560px breakpoint dropping the wordmark, the progress bar and the presence flag so the pill's full `locked / total` ratio and the pseudo both survive a phone width, a 420px one tightening the paddings).
Why: what leaves is either read rather than acted on (shrunk to phone width, a contributors row showed 5 to 9 characters of a name and an activity line a truncated stub) or already answered by a gesture: pinch and double-tap zoom the board, and the overview jumps across it. Height gates it as much as width because a phone held sideways is ~375px tall: the HUD that fits its 812px portrait rail needs some 700px there and has 323. Gating in JS instead of `display: none` means none of them mount, so standings deltas and relative-time ticks do no work behind a hidden panel on the weakest hardware the site runs on.
Revisit when: three things have no compact entry point: the full-board modal and the scoring explainer (both hang off the contributors panel), and `fit`/`center`, which only a deliberate pinch out to the zoom floor stands in for. Give them an account-menu entry, or a real mobile nav (tab bar / drawer) per the backlog item, if any is missed. Verified by measured rects in a dev `/play` at 375x812, 812x375 and 320x568 and back up to 1920x1080, never on a real device.

### 2026-08-07, image-pipeline, tile target cut from ~50 000 to ~10 000 by widening block size

Choice: `build-mosaic.ts`'s recommended `--min-block`/`--max-block` for the real production run move from 3-6 to 8-12 (average region ~100 pieces instead of ~20), cutting the tile count the global no-repeat constraint requires from ~50 000 to ~10 000 for the full 1000x1000 grid. Verified directly: `8-12` produces 9 686-10 092 regions across three seeds at 1000x1000.
Why: ~50 000 distinct CC0/PD or manually-vetted photos was impractical to source at all (see the entries above and below); ~10 000 is reachable from a combined Wikimedia + Unsplash library within a reasonable amount of manual effort, at the cost of larger, chunkier stamps.
Revisit when: never expected; `--min-block`/`--max-block` stay caller-tunable if the real tile library ends up smaller or larger than planned.

### 2026-08-07, image-pipeline, Unsplash tiles are ingested from manual downloads, never fetched automatically

Choice: `ingest-unsplash-tiles.ts` only organizes photos already sitting on disk (matching Unsplash's own download filename convention, `<photographer>-<11-char-id>-unsplash.<ext>`) into the same `manifest.json` `fetch-tile-images.ts` writes; it never calls Unsplash's API or downloads anything itself. The actual download is a manual step: Unsplash+'s "Download All" button on a collection page, done by a person in a browser.
Why: Unsplash's own API guidelines state the API is for "non-automated, high-quality, and authentic experiences" and that applications "must use hotlinked image URLs... for all image uses" (both guidelines cut against an automated bulk-download script permanently embedding photos in a self-hosted derived image); confirmed firsthand that "Download All" itself silently fails above 1 000 photos in a collection (undocumented anywhere on Unsplash's side), so collections were hand-picked under that cap during planning rather than assumed unlimited. Per-photo license (standard vs. Unsplash+-exclusive) isn't recorded, only a generic "Unsplash" marker, since neither license requires attribution and the filename alone doesn't disclose which catalog a photo came from.
Revisit when: never expected; if Unsplash ever exposes a documented bulk-export path that doesn't run into the same automation and 1 000-photo constraints, reconsider automating this step too.

### 2026-08-11, image-pipeline, build-mosaic.ts decodes each tile on demand instead of pre-caching the whole used set

Choice: `renderBandChunk` decodes and resizes each region's assigned tile straight from disk at the moment it's rendered; the earlier `buildStampCache` pass (decode every used tile up front, `fit: "inside"`, hold all of them in memory for the whole render) is removed entirely. The final assembly is also no longer one flat `composite()` of every band strip: `mergeStrips` recursively merges them in groups of `branch` (4), bounding how many real large tiled inputs any single libvips composite call handles at once, with `bigtiff: true` on every intermediate (not just the final file), since later merge levels keep stacking them past classic TIFF's 4GB ceiling.
Why: `selectTiles`'s no-repeat constraint means each tile index is ever assigned to exactly one region, so the whole-library cache bought no reuse, only held gigabytes of redundant buffers in RAM for nothing; at 120px/piece (9982 used tiles, up to 1440px stamps) this alone OOM-killed the container. The flat 100-strip composite was a second, independent failure once the cache was fixed: it segfaulted on Windows regardless of content (even a blank same-size write did) and OOM-killed on Linux with the default Docker memory allocation, so bounding real-input fan-in was needed either way, not just a memory bump.
Revisit when: never expected; `branch` is a plain constant, generous at this scale (100 strips), reduce it if a much larger grid or piece-size ever OOMs again despite the Docker memory bump below.

### 2026-08-11, image-pipeline, real production builds run in Docker with WSL2 memory raised to 12GB

Choice: `docker-run-mosaic.sh` (an uncommitted local script, not part of the repo) copies the repo into the container's own filesystem rather than bind-mounting it directly, so `npm ci` never writes Linux-native binaries (sharp's build) into the Windows-visible `node_modules`; only the tile/main image inputs and the output/temp directories are bind-mounted. `C:\Users\<user>\.wslconfig` sets `memory=12GB`, up from WSL2's undeclared ~8GB default, since the merge phase genuinely needs several GB of headroom per operation at 120px/piece and the dev machine's other running containers (the project's own compose stack) turned out not to be the bottleneck. Free host disk margin needs to be generous, not just sized to the final output: WSL2's dynamic `docker_data.vhdx` does not reclaim space in real time as the script deletes its own intermediate merge files inside the container, so host-visible free space can be consumed far beyond the ~20GB final BigTIFF while a run is in progress (observed ballooning to 69GB mid-run, twice, on a re-run of the real 1000x1000@120px grid), and once the host disk actually hits 0 the WSL2/Docker daemon wedges (`docker ps`/`docker info` hang indefinitely) rather than failing cleanly.
Why: this is the same libvips write-ceiling bug as the entry above, Docker is required for any real-scale image-pipeline run on this machine, not just a one-off for this mosaic. `slice-image.ts`'s full-scale run needed Docker for the same reason (its whole-puzzle-area DZI extract is a single vips image object over the full source dimensions, the same pattern that breaks on Windows past ~5 GP), though it ended up reading/writing through a native Docker volume instead of this script's bind-mount, for unrelated reasons: see [Earth Mosaic slice and upload](#2026-08-15-image-pipeline-earth-mosaic-full-scale-slice-and-upload).
Revisit when: `docker-run-mosaic.sh` or an equivalent becomes a real committed script if this stops being an occasional manual step; the WSL2 memory bump is a one-time machine setting, not per-run. If the daemon does wedge from disk exhaustion, recovery is `wsl --shutdown` then restart Docker Desktop; actually reclaiming the bloated VHDX back to the host needs `diskpart` (`select vdisk file=...` / `attach vdisk readonly` / `compact vdisk`) run from an **elevated** shell, which an ordinary session cannot self-grant.

### 2026-08-13, image-pipeline, tile selection penalizes flat, mostly-empty-black tiles and declusters the ones still used

Choice: `selectTiles` scores each candidate tile by Lab distance plus a penalty when the tile's cached black-pixel fraction (share of near-black pixels in a small downsample, `computeBlackFraction`) exceeds 0.5, and a second, larger penalty on top of that when an already-decided neighboring region (via a new `computeRegionNeighbors` adjacency map over the irregular partition) also picked a flat tile. Both penalties are soft, in the same squared-Lab-distance units the ranking already uses (~15 dE76 and ~35 dE76 equivalent), not a hard exclusion: a flat tile still wins when it is clearly the best match and nothing reasonable competes.
Why: requested after reviewing a preview render: photos that are mostly empty black space around one small bright subject (NASA's Unsplash uploads: planets, the Moon, Hubble frames) read as flat once stamped down at region size, and clumped together where they're the color-nearest match, in the deep-space-black parts of the source image. Plain pixel stdev doesn't separate these from ordinary photos (measured: both a NASA sample and a random sample average ~46 stdev on a 64x64 downsample), but black-pixel fraction does cleanly (NASA sample averaged 54% near-black pixels vs. 18% for the rest of the library), so that's the signal used, computed and cached alongside the existing Lab color average (a separate decode, not reusing the same downsample, so the already-tuned color-match numbers can't shift as a side effect).
Revisit when: never expected; verified in the small (119-region) preview scale where the penalty alone was enough to avoid flat tiles entirely (0 used, dE76 rose from 0.71 to 5.20), and in the real 1000x1000@120px run where the whole ~10.7k-tile library is nearly fully consumed and some flat tiles are still the only reasonable option (1479/9982 regions, dE76 18.47 vs. the unconstrained selection's own natural match quality at that scale). The 0.5 threshold and the two penalty constants are eyeballed against this one library, not tuned against a larger sample; retune by eye if flat tiles still read as too frequent or clustered in the final output.

### 2026-08-15, image-pipeline, earth-mosaic full-scale slice and upload

Choice: the real production puzzle (`earth-mosaic`, a freshly generated seed validated at 1000x1000: 1 000 000 pieces, 1 998 000 unique edges, 0 collisions) was sliced full-scale through the unchanged `slice-image.ts` and uploaded to R2 (`mpp-assets/earth-mosaic/`), both run locally in Docker rather than on the VPS. Unlike `docker-run-mosaic.sh`, the source TIFF and the slicer's output were read/written through a native Docker volume, not the Windows-bind-mounted `generated/` directory.
Why: two independent bottlenecks stacked on the obvious path. The Windows/WSL2 bind mount is I/O-latency-bound for ~1.3M small files (an initial run against it managed only ~22 pieces/s, CPU pinned at ~4/16 cores regardless of libvips concurrency); moving both the source and the output onto a volume removed that translation layer entirely. Even then, throughput stayed capped at the same ~4-core ceiling until `UV_THREADPOOL_SIZE` was raised past its Node default of 4: sharp/libvips dispatches CPU-bound work through libuv's threadpool, so no amount of `--concurrency` at the JS level crosses that cap. `UV_THREADPOOL_SIZE=16` alongside `--concurrency=16` fully saturated the machine (~62 pieces/s sustained), finishing in a few hours instead of an estimated 40+.
Revisit when: never expected for this exact pipeline again; if a future full-scale slice or mosaic build regresses to single-digit pieces/s, check the bind-mount and threadpool-size causes here before assuming it's a slow machine. Upload was verified byte-identical to the local source (1 298 880 objects, 5 988 035 193 bytes) and publicly reachable through the CDN. Prod switched to serve `earth-mosaic` 2026-08-15: its seed added to `MPP_ADMIN_PUZZLES` in the Coolify env, cut over via the admin ops page (full wipe, then switch-puzzle; see admin ops page above).

### 2026-08-16, infra-deploy, self-hosted analytics via Umami

Choice: Umami (self-hosted, Postgres-backed) provides `/play` analytics (unique visitors, average visit duration, country, referrer, new-vs-returning), not Cloudflare Web Analytics, Plausible, or a custom Redis-backed build.
Why: Cloudflare Web Analytics has no visit-duration metric or self-exclusion, and its "Visits" figure is a referrer heuristic, not a real deduplicated count; Plausible CE needs ClickHouse (2-4GB RAM minimum), too heavy alongside this project's own Node/Mongo/Redis/Coolify already sharing one VPS-3; a custom build was scoped and rejected as more than this needs. Umami's native dashboard, plus its Retention report for returning-vs-new, covers every metric asked for, self-hosted in ~200-500MB (Node + Postgres). Visitor identity is a hash of IP, browser, and site, re-salted monthly rather than daily, so a returning visitor across this project's weeks-to-months event still counts as returning rather than reading as new on every visit.
Revisit when: never expected at this scale; `VITE_UMAMI_URL`/`VITE_UMAMI_WEBSITE_ID` are unset outside prod, so no build but the real deployment ever sends events.

### 2026-08-16, infra-deploy, Umami deployed as an unmanaged compose stack outside Coolify

Choice: Umami + Postgres run as a plain Docker Compose project (`~/umami` on the VPS), not a Coolify-managed resource, deployed via direct SSH per an explicit operator request; the container sits on both its own compose network (shared with its Postgres) and the existing `coolify` network, pinned to the latter by a `traefik.docker.network=coolify` label, and carries a hand-written Traefik HTTPS router (shared `letsencrypt` resolver, same pattern as the app's own), with no HTTP-to-HTTPS redirect router of its own: plain `http://analytics.millionpiecepuzzle.com` gets a 404 rather than a redirect. The only self-exclusion mechanism is a link on the Basic-auth-gated `/admin` page (deep-links to the frontend with `?umamiOptOut=1`, setting Umami's own `umami.disabled` localStorage flag), not a public toggle.
Why: mirroring the app's own already-observed label/network pattern was simpler and lower-risk than reverse-engineering Coolify's API for a managed resource. The network pin is not optional: Coolify's proxy runs without a global `providers.docker.network`, and Coolify's own containers never need the label because each sits on exactly one network that the proxy is attached to. This stack is the only multi-network one on the host, so Traefik's choice of backend address was free to change on any of the routing-table rebuilds it performs on every Docker event, and it eventually settled on the compose-internal address the proxy has no route to, hanging every request into a Cloudflare 504 (2026-08-30, flipped by the nightly `ServerPatchCheckJob` and `DockerCleanupJob` container churn at 00:00 UTC). The redirect router was dropped after it broke the whole host (Traefik couldn't build any router for it, a full TLS-layer hang surfacing as a Cloudflare 504): it pointed at a bare `redirect-to-https` middleware name that isn't stable outside Coolify's own per-resource label generation, so it stopped resolving as soon as the app's own redeploy regenerated that middleware under a different name; Traefik's ACME HTTP-01 handling doesn't depend on a user-defined router, so dropping it doesn't affect certificate renewal. Operator-only exclusion was an explicit choice, keeping the toggle out of reach of regular players while still covering dev/test traffic, so the privacy policy relies on the existing GDPR contact-the-operator right rather than advertising a self-service opt-out.
Revisit when: Umami should become visible/restartable from Coolify's dashboard like every other service (import `~/umami/docker-compose.yml` as a resource then, and repoint the backup sidecar, which attaches to this project's `umami_default` network by a name that follows the compose project directory), or a visitor asks for their own opt-out (add a public toggle to `OptionsModal.vue`; `consumeAnalyticsOptOut` already supports it).

### 2026-08-17, frontend-shell, brand mark is a raster PNG, not a vector path

Choice: `BrandMark.vue` renders `public/brand-mark.png` (transparent background, cropped tight to the shape) via `<img>`, not an inline SVG path. `scripts/generate-icons.ts` composites that same file onto cream tiles for `favicon.png`, `apple-touch-icon.png`, and `discord-icon.png`.
Why: the source artwork exists only as a raster export; treating it as the asset of record preserves it exactly, whereas hand-tracing it into a path risked drifting from the real shape.
Revisit when: the mark needs a dynamic fill color (theming, dark mode) or a render size large enough for raster upscaling to visibly blur; trace it into a proper vector path then.

### 2026-08-17, frontend-shell, SEO metadata is English-only, corrected per route in JS rather than per locale

Choice: `index.html`'s title/description/canonical/Open Graph/Twitter Card/JSON-LD are static English tags, sized for the landing page. `src/seo.ts` overwrites title/description/canonical/robots on every `router.afterEach` so `/play`, `/privacy`, and `/legal` each get their own values (and `/play` gets `noindex`) instead of inheriting the landing page's, but every locale still renders the same English strings.
Why: locale is chosen client-side (`localStorage`/browser language, see `i18n/index.ts`) with no URL segmentation, so a search engine can only ever index one language version of `/`; matching non-English queries (e.g. French "puzzle le plus grand") word-for-word needs its own crawlable URL per locale, a router restructuring out of scope for a metadata pass. A fully static canonical was rejected once picked apart: baked into the one shared `index.html`, it would otherwise assert on every route that `/privacy` and `/legal` are duplicates of `/`, telling Google to drop them from the index.
Revisit when: ranking for non-English queries matters enough to justify locale-prefixed routes; see the ROADMAP backlog.

### 2026-08-18, qa-and-load, load-test bots discover pieces empirically and cluster toward a shared hotspot

Choice: a `Swarm`, one shared instance per run, holds a single discovered `{x, y}` hotspot. Any bot may seed the very first hotspot from the centroid of a `region_state` batch's groups (logged once as `[swarm] hotspot found near (...)`), but once one exists, only clustering bots (`--cluster-fraction`, default 0) keep it fresh; a non-clustering bot's own sightings are ignored from then on. Clustering bots center their next viewport pick on the hotspot plus roughly one viewport-width of jitter instead of a fully random point; non-clustering bots keep the original independent scatter throughout. Two more changes ride along: a bot's drag target now samples from its own current viewport (expanded a little, floored so it's never degenerate) instead of the whole play zone, and its cursor is a small glide/track state machine (eases toward the next grab target, snaps to the live drag position while holding, goes silent once converged so the receiving client's own idle-bob still kicks in) instead of a teleport to a random point every tick.
Why: a bot can't be handed a fixed watch region because it has no legitimate way to know where pieces are ahead of time; the scatter layout derives from `generationSeed`, which is server-only and never sent over the wire (see anti-programmatic-solving above). Looking around and reporting what's found is the same discovery a real player does, just fed into one shared spot instead of one player's memory. The first real run (12 bots, half clustering, against a freshly unsolved `synthetic-1m`) caught a real bug in the original "every bot reports, always" design: with loose pieces everywhere, the 6 independent-scatter bots' own uniformly-random sightings overwrote the hotspot on almost every tick (observed swinging across a >150 000-unit range, several times a second), so the clustering bots never got a stable point to converge on. Restricting ongoing updates to the clustering subset keeps drift to what that subset is actually doing (their own jitter, or a piece they genuinely dragged out of the area) instead of board-wide teleports from unrelated bots. Viewport-local drag targets double as more realistic on their own (nobody drags a piece the width of a 1M-piece board in one motion).
Revisit when: never expected structurally; the clustering-only-updates fix is verified live (see above). The jitter width, the cursor's 25%-per-100ms-tick ease fraction, and the drag-range expand/floor constants are still eyeballed; retune by eye once a run is watched end to end in a real browser.

### 2026-08-18, frontend-shell, HUD panels move to two flex rails instead of independent corner anchoring

Choice: `ReferencePanel`/`ZoomControls`/`ActivityPanel` (left) and `ContributorsPanel`/the `DevControls`+`OverviewPanel` group (right) are children of one `.hud-rail` flex column per side (`display:flex; flex-direction:column; justify-content:space-between`) in `PlayPage.vue`, instead of each independently `position:absolute` with its own corner offset. `.panel`'s shared base class carries no `position:absolute` so its consumers act as normal flex items; each rail is `pointer-events:none` with every child (`.panel`, `.zoom`, `.dev-controls`) re-enabling `pointer-events:auto` so a click in the empty space between panels still reaches the canvas underneath. A rail is bounded on both axes: `--hud-rail-max` (`min(340px, 33vw)`, declared on `.stage`) is the width every panel and the bottom-right group cap their own against, the two aspect-bound panels also cap it against the rail height left for them (`calc((100dvh - <budget>) * var(--ar))`), and the two list panels shrink first and scroll their own list. `FlagBar`, the one element neither column can place, insets itself by `--hud-rail-max` on both sides and wraps inside the strip that leaves.
Why: reported bug (reference panel covering the zoom controls on a laptop-height screen) traced to independent absolute positioning having no way to know a sibling's actual rendered size. A per-panel `max-height` tuned to today's known sizes was considered and rejected: it would have baked in the current button count, the current activity-entry cap, and today's square puzzle aspect ratio as implicit constants. `justify-content: space-between` instead centers the middle child in whatever room the other two actually leave, which holds for any panel height and any viewport size, by construction. A third of the screen per rail rather than half is what leaves a middle strip at every width: at half, the two rails meet around tablet widths and a bottom-centered bar has nowhere to go but under them. The aspect-bound panels cap a width rather than clamp a height so the thumbnail and the map keep the exact shape of what they show, where clamping the height alone would letterbox them.
Revisit when: never expected for the overlap property itself. Verified by measured rects (every HUD box against the stage and against each other) in a dev `/play` across 13 viewport sizes from 320x568 to 1920x1080, with the panels filled to their caps (a full ticker, 40 standings rows, 8 flags); the session behind it was stubbed rather than live, and no real device was involved. If a HUD element ever needs horizontal centering instead of a left/right rail slot, it needs its own layout mechanism, not a forced fit into one of these two columns.

### 2026-08-18, auth-and-accounts, onboarding skip defaults reuse the existing taken-pseudo retry path

Choice: skipping the pseudo onboarding step generates "Guest #XXXX" (4 random uppercase alphanumeric characters, `generateGuestPseudo` in `packages/shared/src/session.ts`) and submits it through the same path as a typed pseudo, with no pre-check for uniqueness; skipping the country step submits the existing `INTERNATIONAL` code the same way. `PSEUDO_PATTERN` was widened to accept `#` so the generated form round-trips through the same `normalizePseudo` the server also runs.
Why: a collision (409 `pseudo_taken`) already sends the player back to the pseudo modal with an error, so a generated pseudo just reuses that path instead of needing new dedup logic; 4 characters (36^4, ~1.68M combinations) is small enough that collisions become likely well before real usage gets large if many players skip, a risk accepted for a first version rather than building a server-side retry loop or widening the tag before any real skip-usage volume is observed.
Revisit when: repeated 409s on skip-generated pseudos show up at real usage; widen `GUEST_TAG_LENGTH` or add a server-side regenerate-on-collision loop then.

### 2026-08-21, qa-and-load, admission cap set to 200 from a measured single-core ceiling

Choice: `MPP_MAX_ACTIVE_CONNECTIONS=200` in `packages/server/Dockerfile`, arming the admission queue that the code default (0) leaves off. A staircase soak at 200/500/1000 bots against the real board put the server process flat at ~110% CPU on all three, while the host kept 4 of its 6 cores and 10 GB of RAM idle, so the ceiling is the single-threaded event loop and no VPS tier moves it. Grab round-trip was p50 84ms / p95 300ms with 3.7% timing out at 200; p50 457ms / p95 824ms with 43% timing out at 500; at 1000 the server never got past ~623 established connections in five minutes while the rest errored out.
Why: without a cap the box has no safety valve and degrades by accepting arrivals it cannot serve, which reads to a player as a dead connection rather than a wait. Bots hold a cluster ~85% of the time against a human's 10-30%, which argues the real ceiling is higher, but presence broadcast is O(N) per connection and so O(N^2) on a mass arrival, which argues it is lower at exactly the moment a launch stresses it; 200 takes the conservative side of that spread.
Revisit when: the value is read at boot only, so raising it is a redeploy, not a restart. Re-measure once real play has run, and before then if the single-writer topology changes (the ceiling is per-process, so sharding the writer is what actually moves it).

### 2026-08-21, backend-realtime, a grab granted after the client gave up parks the group for MPP_STALE_HOLD_MS

Choice: known and accepted for launch, not fixed: under saturation the server can send `grab_ok` after the client has already timed out its own grab, and the client then ignores it. The server still counts the group as held, so nobody can take it until the 30s stale-hold sweep reclaims it past `MPP_STALE_HOLD_MS` (180000).
Why: surfaced by the 2026-08-21 staircase soak, which logged 8672 `[stale-hold]` reclaims once grab latency passed the harness's 5s patience. It is only reachable above the admission cap's own load level, so the cap is the first line of defense; a real fix (client acknowledging or explicitly releasing a late grant) is protocol work that launch does not block on.
Revisit when: real play shows pieces reading as stuck, or the cap is raised. Note the sweep logs one line per reclaim on the main thread, so a burst is itself synchronous work on an already-saturated event loop.

### 2026-08-23, backend-realtime, a board carrying another generation seed is relabeled at boot, not wiped

Choice: when Redis meta's `generationSeed` differs from the seed the server loads, the boot permutes every stored group origin onto the loaded seed's ids (`relabel.ts`) instead of serving the board as-is. Each id takes the rendered anchor of the group that currently shows its art, corrected by the two solved cells, so every piece keeps its image and its pixel. It refuses (and stops the boot) as soon as anything is merged or locked, since a relabeling cannot rename a frozen cluster, and it snapshots the pre-relabel origins to `puzzle:{id}:relabel-origins` first so an interrupted run resumes instead of applying the permutation twice.
Why: the seed drives the wire id permutation and the R2 tiles are named by `wireId = P(seed, gridId)`, so a mismatch hands every client the art of a piece that belongs to another cell and nothing can ever snap, silently. It happened live: the admin "clear everything" wipe drops `admin:puzzle-override`, so the boot fell back to the env baseline, which paired `MPP_PUZZLE_ID=earth-mosaic` with the load-test `MPP_GENERATION_SEED=synthetic-1m`. Players had already sorted piles by then, and a wipe would have thrown that away for a board that only needed its labels moved. Measured on the live 1M board: 61.7s end to end inside a ~3min boot (19s to read, 0.5s to compute, the rest writing), worst anchor drift 1.5e-11 world units on a 120-unit piece (float64 rounding, nothing else). Verified after the fact against the independently regenerated original scatter: 995 382 pieces exactly as dealt, 4 618 carried over from where players had moved them, and the reported top-right corner landed on the anchor a read-only rehearsal had predicted bit for bit.
Revisit when: never expected once the env baseline carries the real seed. A board with merged clusters can no longer be repaired this way and needs a wipe.

### 2026-08-23, infra-deploy, a restart is announced on the wire and the maintenance page is the app's own screen

Choice: on SIGTERM/SIGINT the server broadcasts `{t:"error",code:"maintenance"}` to every client, closes each socket with RFC 6455 code 1012 (Service Restart), waits 300ms for the frames to leave, then exits. The client treats either signal as maintenance, and so does an unreachable host anywhere on the entry path: `/auth/session` failing or answering 5xx at boot (`backendDown` in `useAuth`, which also holds off guest onboarding), a queue ticket or status doing the same, and `/landing` not answering on the landing page. All of them land on the same localized screen, on the canvas cover and in the landing hero, served by Cloudflare Pages with no backend involved. The parked session then probes `GET /landing` on a jittered 8 to 16s cadence and reloads the page once it answers, rather than reconnecting in place.
Why: the static alternative (a `maintenance.html` plus a `_redirects` catch-all, committed on and off) matches the deployment-config-in-code pattern but not the trigger. One push to `main` redeploys Pages and Coolify together, so arming the switch would itself restart the backend, disarming it needs a third push, it stays on if forgotten, and it does nothing for a crash or an outage. Detection costs no operator step and covers all three. Two signals rather than one because either can be lost: the notice needs the socket to still flush (hence the drain), and the close code needs every proxy in front to relay it. A reload rather than a reconnect because a server coming back is almost always a new deploy whose bundle Pages has already shipped in the same push, so reconnecting in place would keep an old client against a new server (and get `protocol_mismatch` on a protocol bump). The jitter spreads a few hundred parked clients over 8 seconds instead of dropping them on a just-booted process at once, and stays far inside the 120-per-minute public-endpoint window. Verified locally end to end: a connected player mid-board switched to the maintenance screen on `docker compose stop server` (server logged `notifying 1 client(s)`, client logged the notice, never the connection error), and reloaded itself back into a ready board once the container was started again; with the server absent entirely, both `/` and `/play` show the screen and no onboarding modal opens.
Revisit when: a deliberate pause needs the whole site sealed behind its own message (a planned multi-day stop, a takedown), which is what a static Pages page plus a `_redirects` rule would be for; the automatic screen only reacts to the server being unreachable. Also if the container's stop grace period is ever cut near the 300ms drain.

### 2026-08-23, auth-and-accounts, the Google sync links onto the guest document instead of creating a second account

Choice: the sign-in POST carries the live guest session, so Auth.js links the provider account to that same user and never creates a Google document. An `events.linkAccount` handler promotes it in place: `guest` cleared, OAuth email/name/image written, claim token retired. The claim path (`POST /guest/claim`, the token stored at mint, `claimGuest`) stays, and now only spends a stale token since nothing is left to reattribute.
Why: found live, seven accounts deep, every one of them still reading as a guest, so the options menu kept offering the sync and never showed an identity. Linking in place is also the safer of the two repairs: the alternative (clearing the session cookie before the OAuth hop, so a Google document is created and the claim folds the guest into it) is what the claim machinery was written for, but it signs the player out the moment they abandon Google's consent screen, and it moves every contribution row instead of moving nothing. The seven live accounts were backfilled from the identity claims in their stored `id_token`.

### 2026-08-23, auth-and-accounts, the way back into an account is a sign-in before the guest is minted, or a deliberate session drop to switch

Choice: the onboarding pseudo step carries a sign-in entry, the one moment in the entry flow with no session, where Auth.js resolves the provider account to the profile it is already linked to and signs straight into it. A player who skipped it and is already a guest hits Auth.js's refusal instead (`OAuthAccountNotLinked` on the OAuth path Google takes, `AccountNotLinked` on the WebAuthn one, both accepted since it will not attach a linked account to a second profile either way), which a handoff route on the auth host bounces back into the app as a query flag, and the modal offers the switch: sign out, come back to `/play`, leave for Google again with no session, and let the pending claim tokens fold this browser's guest into the account on arrival.
Why: without it a synced account is a dead end the day the 30-day cookie ages out or the player opens another browser. The only sign-in entry sat behind a session, so a returning player was minted a fresh guest, could not even reuse their own pseudo (their own account holds it), and split their contributions across two identities. Three details are what make the switch safe rather than lossy: claim tokens are a list, since a sign-in abandoned on Google's consent screen comes back to a guest mint that must not overwrite the token still waiting; `claimGuest` fills only the identity the target is missing, so an established account absorbing a throwaway guest does not take its "Guest #XXXX" name; and the claim route hands the fold to `LeaderboardTracker.reassign`, since the live standings are in-memory and keyed by user id, otherwise the moved score would only appear at the next boot.
Revisit when: the switch costs two trips through Google (the sign-out, then the sign-in) and only the second is visible to the player. If that reads as a stutter, the alternative is making every sync sessionless, which trades the in-place link for a document swap on every account.

### 2026-08-24, frontend-canvas, personal flags are a per-browser client model with a board-scaled canvas glyph

Choice: up to 8 flags live in `localStorage` under `mpp.flags.<puzzleId>`, never on the wire and never on the account. Colors are a permutation of a fixed 8-entry palette (taking a color another flag holds swaps the two), which makes the color the flag's identity across the HUD bar, the canvas and both overviews, and makes the cap and the palette the same number. `FlagLayer` is a child of the Pixi root rather than of `world`, but `update()` scales every glyph by the camera, so a flag stands one and a half pieces tall at every zoom. A flag is created from a `+` control in the bar at the center of the current view, not by a gesture on the board, and the bar is not rendered on a compact viewport. `applyCamera` re-places a sticky-carried cluster at `lastPointerScreen` before its `reconcile`, so every camera change re-glues it, not just the flag jump.
Why: personal, low-stakes and worthless to anyone else, so an account-backed model would buy cross-device sync at the cost of a schema field, a route and a sync path; the board coordinates it stores are already client-visible either way. Screen-space parenting keeps the glyph over the zoom-out LOD bands, which bake `world` into tiles and would otherwise swallow it, while the per-frame scale keeps it reading as part of the board instead of as an overlay that swells to cover the page at 15% zoom. A `+` button rather than a double-click on the canvas because the double-press is already the sticky-carry pick-up and drop, and a gesture that only works with an empty hand is a gesture that fails exactly when a player is carrying a piece to a flag. The re-place is what makes the feature worth having: a carried cluster is otherwise only moved by a pointer move, so the teleport would strand it behind, and `lastPointerScreen` survives the pointer leaving the canvas for the bar (`pointerScreen` does not), so the cluster holds the screen spot it was already floating at. It has to happen before the reconcile, not after: residency runs there, and a cluster still at the old spot is outside the new keep ring, so it was dehydrated and nothing re-hydrated it until the camera next moved (a settled frame skips the candidate pass), leaving an empty hand. Verified on a local board at 500% zoom: with the re-place neutralized the carried cluster's piece nodes were destroyed on the jump, and restored the cluster stayed hydrated, resident and inside the new viewport.
Revisit when: players ask for their flags on a second device, or 8 stops being enough, at which point the palette-as-identity trick is what has to give first.

### 2026-08-24, frontend-canvas, the reference underlay mirrors the reveal layer's own tiles, and locked content casts a shadow on it

Choice: the underlay fetches nothing. `DziRevealLayer` mirrors every DZI tile it already streams, the always-resident base layer and the zoom-level tiles alike, into a second container in its own world layer (above the frame, below every piece), sharing the same textures. Its opacity is a group opacity, one `AlphaFilter` at 0.32, not the container's `alpha`. Locked content is lifted off that ghost by one further draw of the combined mask, tinted `0x1a1a1a` at 0.45 alpha and offset 3 screen pixels, under the locked tiles. The player's choice lives in a display-settings store persisted per browser under `mpp.display`, on by default.
Why: the ghost has to be the same photo the locked pieces reveal, at the same level and the same subpixel alignment, or the aid would drift out of register with the board exactly where a player is comparing them; mirroring the sprites gets that for free and costs no fetch and no VRAM, only its own draws. Group opacity rather than container alpha because Pixi multiplies a container's alpha into each child separately, and the ghost is several layers deep over one spot (base tiles under zoom-level tiles, plus a stale level kept as a fallback across a zoom step), so a nominal 0.28 measured 0.48 in steady state and 0.63 mid-zoom and the aid visibly darkened and lightened as tiles streamed; the filter flattens the container once and its render target is clamped to the viewport, so it costs one screen-sized pass and only exists while the aid is on (measured after the fix: every ghost pixel at exactly 82/255). Reusing the mask as the shadow is what makes the separation affordable: the silhouette is already baked and already on the GPU, so the whole board's frontier and every hole cost one sprite, where a per-piece shadow would not survive the zoom-out LOD band, which bakes tiles. One trap on that reuse: a Sprite only rebuilds the quad it batches when its texture is reassigned or fires an update event, and the mask `RenderTexture` is resized in place on every real ring move, so the shadow kept drawing at the previous dimensions and put a whole second, displaced copy of the locked slab on the board; the sprite is dropped at the resize and rebuilt on the same frame (measured across five zoom levels afterwards: 0-2 px from the intended 3 px offset).
Revisit when: 0.32 was set by eye on a synthetic board whose source image is nearly flat, and the shadow was only ever exercised locally against one hand-injected baked mask, since a dev stack with no R2 write credentials composites nothing and therefore renders no locked pieces at all. Both want a second look on the real photo, and more urgently now that the aid is on by default: every player sees the ghost before ever opening the menu, and pays its one screen-sized filter pass per frame. If it reads as noise against the real photo rather than help, that default is the first thing to reconsider.

### 2026-08-24, frontend-canvas, a cluster is sent to a flag by dropping it on the HUD bar, onto a searched clear patch

Choice: a press-drag released over a flag button in the bottom-center bar moves the cluster to that flag and leaves the camera alone; a sticky carry is unchanged, its press on a button still being the click that jumps the camera. The bar registers its buttons as a pull source read once per grab, in client coordinates, and the stage hit-tests the pointer against those rects itself, which is what keeps the canvas and overview markers out of the gesture. On release the cluster is placed on an expanding square-ring search around the flag, each ring walked nearest-first, against the locally known clusters and locked pieces, falling back to the flag's own patch; where it actually rests is the server's own answer to the same search (see the server-resolved landing spot below).
Why: the two gestures have to stay distinct, and the button held is the only thing that separates them: a carry holds none, so it has no release to read and its click already means "take me there". Measuring on grab rather than per frame is enough because the bar cannot relayout under a held pointer, and client coordinates are the one frame the DOM bar and the canvas both speak. Two smaller consequences fall out of the bar being DOM over WebGL: edge-pan is suppressed while a button is hovered, since the bar sits inside the bottom edge band and would scroll the board out from under the drop, and the bar thins to 26% opacity while it holds a cluster, since no stacking order can lift a canvas draw above a DOM overlay and that is the closest the piece gets to passing over the flags.
Revisit when: the gesture only exists where the bar is rendered, so a compact viewport (a phone either way up) has flags neither to drop on nor to create, and a player who fills their bar on a desktop finds no way to use it from a phone.

### 2026-08-24, backend-realtime, live standings ride a coalesced delta, with a personal standing for anyone outside the top N

Choice: every merge that credits a piece marks its contributor dirty and `LeaderboardBroadcaster` publishes at most once per `MPP_LEADERBOARD_BROADCAST_INTERVAL_MS` (2s), the first merge of a quiet window going out immediately. A publish broadcasts `leaderboard_delta` with only the moved rows that sit inside the top `LEADERBOARD_LIMIT`; the client folds them into the list it already holds, re-sorts on the shared `compareStandings` and truncates back to the same bound. A contributor who scored while ranked outside that top N gets `standing` (tally plus competition rank) on their own socket instead, as does one the same publish pushed out of the top N (diffed against the previously published id set, so the extra sends stay bounded by the rows that entered), and it is sent on join too; the full list still goes out on join and on a guest fold, the one change a delta cannot express since a retired row has nothing to send.
Why: the standings only reached clients on an anchoring lock, so a merge joining two loose clusters scored silently until a page reload. Lifting that gate as-is would have pushed the whole top 100 with pseudo and country (~8.5 KB) to every client on every merge; the delta is a fraction of that and still bounded by the top N in the worst case, and the coalescing turns an unbounded per-merge fan-out into one publish per window. A personal message rather than a wider broadcast because exactly one client cares about a row ranked 4000th, and that rank costs one sorted pass over the tallies plus a binary search per recipient, never a scan each. Additive on the wire with no `PROTOCOL_VERSION` bump, so an old client keeps playing against a new server, which matters when Pages ships the frontend on push and the backend is a manual redeploy. Verified end to end on a local board: a non-anchoring merge published its delta 17ms later, three merges 100ms apart published twice (immediate, then one coalesced flush at +2047ms carrying the accumulated tally), and a connected browser's panel re-sorted live.
Revisit when: the 2s window is eyeballed. A board busy enough that most windows carry near the full top N is sending the whole list anyway, at which point the delta stops paying for itself.

### 2026-08-25, frontend-canvas, the locked-content mask is an owned MaskFilter clipped to the viewport, and a zoom-in makes a bake stale

Choice: `DziRevealLayer` masks `tileContainer` by owning Pixi's `MaskFilter` around the combined-mask sprite and setting it as the container's only filter, with `clipToViewport` turned back on, rather than assigning `tileContainer.mask`. A bake also counts as stale once the rect it covers is more than `MAX_COVER_SLACK` (2x) the current keep ring, not only when the ring escapes it.
Why: `container.mask` routes through Pixi's `AlphaMaskPipe`, whose `MaskFilter` pins `clipToViewport = false`, and `FilterSystem` then sizes the pass's render target from the masked container's own global bounds. Those bounds are world content scaled by the camera, so they grow without limit as the camera zooms in: driving the real layer from `MIN_ZOOM` to `MAX_ZOOM` on a 1600x900 canvas at dpr 1, the request went from 4480 px to 10160 px wide, which `TexturePool` rounds up to a 16384x16384 (1 GB) texture, and the coarse DZI tiles that feed those bounds are legitimately resident (eviction is spatial, so a level left behind by a zoom step stays as a visible fallback and spans ~10000 screen px by `MAX_ZOOM`). Past the GPU's `MAX_TEXTURE_SIZE` the allocation fails outright, the framebuffer keeps a zero-size attachment, and every clear and draw into it fails: in prod, `INVALID_VALUE: texImage2D: width or height out of range` followed by hundreds of `GL_INVALID_FRAMEBUFFER_OPERATION ... Attachment has zero size`, the locked photo blank under an intact seam overlay, and the driver retrying the same failed allocation every frame. Owning the filter is the only way to reach that flag, and it also bounds the steady-state cost, which was a render target of `nextPow2(2.8 x screen x dpr)` (8192x4096 measured) cleared and redrawn every frame for a pass that can only ever show what fits on screen. Verified pixel-identical to the mask path, alpha edges included, at three zooms. The staleness rule is the other half: containment alone never fires on a zoom-in, so the mask kept the rect and the texel density of the zoom it was first baked at, which on this board is one texel per ~33 screen pixels of silhouette after a full zoom-in from the opening view.
Revisit when: `clipToViewport` is a documented `Filter` option but Pixi ships `MaskFilter` with it off, so a Pixi upgrade that changes the filter-bounds path wants a look at this layer; the mask pass is the one place in the app that depends on a non-default value.

### 2026-08-25, frontend-canvas, a peer pointer expires after 10s of silence

Choice: `PeerCursorLayer` hides a peer whose last cursor is older than `STALE_AFTER_MS` (10s) and places the next one it receives outright instead of easing to it. The peer stays registered with its pseudo, and the pointer comes straight back on the next position; no fade, no protocol change.
Why: a cursor is the only broadcast scoped to a point (a zero-size rect, so a single world cell), so a peer who pans, jumps or zooms out of the receiving viewport's cells goes silent with nothing on the wire saying so, and nothing on this side expired what they left behind. The pointer sat in view for the rest of the session, bobbing its idle animation, which prod read as the other players' cursors freezing until a reload. Placing the returning position outright rather than easing avoids gliding the pointer across the board from a spot the peer left minutes ago.
Revisit when: 10s is eyeballed. The cost is the idle bob, which used to mean "here but not moving" for as long as the session lasted and now means it for 10s, so a real session showing players read as gone while they are still watching wants a longer value.

### 2026-08-27, frontend-shell, the update-notes dot seeds a new browser as read

Choice: `mpp.updatesSeen` holds the newest release this browser was shown the notes for, and the dot on the gear and on the menu row is `stored !== RELEASES[0].version`. A browser with nothing stored, or holding a value that is not a version, is written the current release at module load rather than marked unread. Only opening the notes clears the mark; opening the menu that links to them does not.
Why: treating absence as unread puts a dot on the settings menu of every first-time player, the one player for whom nothing is new. The cost is one release: the one shipping the dot cannot announce itself, since no browser has a stored version yet, while every release after it can. Equality against the newest version rather than an ordering, since the only move to catch is a release published after the last one read. Per browser like the display preferences: a changelog is not worth a round trip, and nothing on the session user says when the account was created.
Revisit when: the mark has no expiry, so a player who never opens the menu carries the dot until they do. If that reads as nagging once the event has real traffic, drop it after a couple of weeks instead.

### 2026-08-27, frontend-shell, a switched-off HUD panel is hidden in place, not dropped from its rail

Choice: `DisplaySettings.panels` holds one boolean per panel the player has actually switched and nothing for the rest, `isPanelVisible` reading an absent id as on wherever the viewport has room for the panel at all (`isPanelAvailable`). The three left-rail panels always render on a full viewport, and a switched-off one takes `visibility:hidden` with its `backdrop-filter` and `box-shadow` cleared alongside; the contributors panel, the overview and the flag bar are left out of the DOM instead.
Why: storing a full six-boolean record would have to commit every default at first write, on whichever viewport happened to be open, so a phone opened after one desktop session would inherit a full HUD; keeping absence meaningful lets an untouched panel follow the screen it is on, in both directions. The split between hiding and dropping follows what a panel's box holds up: the zoom pillar is the middle child of a `space-between` column, so it is placed by the room its two neighbours leave and one of them leaving the flow slides it somewhere else on screen, while nothing at all is positioned against the other three (the right rail's bottom group carries its own `margin-top:auto`, and the flag bar is absolute). The paint properties are cleared rather than left to the visibility rule alone: a panel-shaped smear of blur and shadow over the board is exactly what a hidden panel must not leave behind. `ReferencePanel`'s and `ContributorsPanel`'s modals sit inside their `<aside>` (they teleport to the body either way) so each component has the single root a class from the rail needs.
Revisit when: the two hidden left-rail panels stay mounted, so an invisible ticker still re-renders its six rows; it is small, but a heavier panel in that column would want the slot held only while the pillar is on.

### 2026-08-27, frontend-canvas, the overview's expand opens the same map, enlarged

Choice: `overviewView.ts` owns the whole overview paint (out-of-bounds band, play zone, server density grid, live piece dots, frame, frustum, flag markers) and its play-zone-plus-band aspect; `OverviewPanel.vue` and `OverviewModal.vue` each hand it a canvas every frame and differ only in size. The modal sizes itself to `min(90vw, 880px, (100dvh - chrome) * aspect)` so the map fits both axes, and the panel stops painting entirely while it is open. Both take the same press-and-sweep camera jump (`useOverviewPointer`, inverting the mapping the painter hands back), under the plain arrow cursor rather than a grab hand.
Why: at 248px wide (152 on a phone) the panel is precise enough to aim a jump with and too small to read the board on, which is what the expand button is for; a second view drawing the same board its own way is a drift risk for no gain, and the map is a picture of where things are, not a surface to pick up. A map that reads better is also the one worth aiming from, and it shows the frustum it moves, so the jump is a live view of its own effect rather than a gesture the player has to close the modal to see the result of. The cursor followed a player report: a hand promises a drag of the map and hides the point the click will actually land on, while an arrow's tip is the aim. Pausing the panel keeps the open modal costing what the panel alone costs: one paint is `O(grid cells + known pieces)`, measured on the local 1M board at 5.1ms for the panel and 5.3ms for the modal with ~3 600 known pieces, so painting both every frame would spend two thirds of a frame's budget on a map that is drawn twice.
Revisit when: the panel and the modal now paint identically, so any per-view treatment (a denser dot at the larger size, labels the panel has no room for) has to enter the shared painter as an explicit option rather than by editing one call site. A single paint also scales with the known-piece count (~1.1us per dot), which grows as a player visits regions; the ceiling is the panel's own, not something the modal adds, but it is the number to watch if the overview ever starts costing frames.

### 2026-08-27, frontend-shell, the landing's progress percentage is floored to three decimals

Choice: `flooredPercent` floors the locked/total ratio in integer space (`floor(part * 100 * 10^d / whole) / 10^d`) at three decimals, and the landing formats it through `Intl.NumberFormat` with `style: "percent"`, dropping to no decimals only at exactly 100.
Why: rounding read `100.0%` from 999 500 locked, so the whole endgame announced a board that still had 500 pieces loose; a floor cannot reach 100 before the last piece at any precision. Three decimals is the coarsest that still moves on a 1M board (one step is 10 pieces, against 100 at two decimals, which would also sit on `0.00%` for the first hundred). The floor takes the integer ratio because flooring the float percentage lands one step low on 12 218 of that board's 1 000 001 counts (70 locked reads `0.006%` where the exact floor is `0.007%`). Intl owns the sign so fr, es and de get the no-break space they want before it and the message strings carry no literal `%`.
Revisit when: three decimals is finer than a single piece below 100 000 pieces, so a smaller board would want its precision from the piece count rather than fixed.

### 2026-08-27, backend-realtime, the server resolves where a flag drop lands, the client's own search left as the optimistic placement

Choice: a cluster released on a flag button is sent as `drop_near`, carrying the flag's plain world point rather than a group position, and the server answers with the landing origin: `resolveDropNearOrigin` walks the ring search both ends now share (`freeSpot.ts` in `@mpp/shared`, same gap, same 20 rings, same centering) against `GroupIndex` and `LockedPieceIndex`, then hands the result to the ordinary `scheduleDrop`, merge detection, tile cap and broadcast included. Both indexes hold a piece's grid box while the client searches with the box it draws, one manifest tile margin wider on every side, so the server grows the cluster by that margin (which is what sets the stride between landed pieces) and grows every occupancy query by it again, standing in for the margin its neighbours' stored boxes are missing. The per-tile piece cap enters that search as its own constraint (`hasRoom`), so a candidate whose destination cell is full is skipped even as the last resort. `GroupIndex` stores each group's body rectangle alongside the origin it reports, and `overlapsBox` starts its cell walk back by the largest extent ever indexed, since a group is keyed only by the cell of its top-left corner. The client keeps running the same search on release, now purely as the placement the authoritative `drop` corrects. Additive on the wire with no `PROTOCOL_VERSION` bump, so the server ships first and an old client keeps playing.

Why: the client can only avoid what it has streamed, and a flag is most useful exactly where the player has never looked, so the search there had nothing to avoid and stacked the cluster on pieces it could not see. Resolving it server-side walks one or two cells of an in-memory index and answers inside the `drop` broadcast that already goes out: zero extra bytes on the wire, and no new client message for the correction, since `applyRemoteDrop` already applies the authoritative position and the broadcast covers the grab AABB, so the dropper receives it even though the camera never left the spot they were working at. The rejected alternative was prefetching the flag's region to the client on the first drop: on `earth-mosaic` (1000x1000 at 120px) the scatter band holds roughly 57 groups per world cell and 100 at its dense middle, so that ships 30 to 80 KB per drop through the very backpressure path `region_state` batching exists to protect, and it goes stale straight afterwards. It also cannot reuse the `viewport` message, which would move the subscription off the player's own area and abort the flag's stream through the `regionStreamSeq` supersession guard the moment the real viewport is sent back. Treating the cap as a search constraint rather than a rejection is what stops a flag standing in a full tile from bouncing the cluster back to where it was picked up, which read as the flag refusing the piece.

Revisit when: the index grew four numbers per group (~32 MB at the 1M-singleton start, shrinking with every merge) to answer overlap without a Redis read, and its extent bound only ever grows, so merging away the widest cluster leaves later queries walking cells that can no longer hold anything. The search itself still ends after 20 rings, so a neighbourhood full in every direction lands on the flag's own patch and can still bounce on the cap.

### 2026-08-29, frontend-shell, a /play window is keyed by its UI name, the wire keeps its own

Choice: each window on `/play` carries one key across its component filenames, its root CSS class, its i18n namespace and its HUD panel id: `reference`, `zoom`, `activity`, `contributors`, `overview`, `flags`. The names the protocol uses for the same data stop at the message boundary and are left alone (`minimap` and `leaderboard` messages, `MinimapGrid`, `LeaderboardEntry`, `mergeLeaderboardDelta`, `PuzzleStage.setMinimapGrid`). Renaming two panel ids drops whatever `mpp.display` held under `minimap` and `leaderboard`, since `parseDisplaySettings` ignores an id it does not know, so a player who had switched either panel off finds it on once.

Why: the panel was titled "Overview" while its accessible name, its Display switch, its tip, its update note and its whole module tree said "minimap", and the standings panel read "Contributors" over a `leaderboard` key everywhere behind it. Carrying the rename into the wire tags would have changed nothing a player sees and cost a backend-first deploy, a client on the new tag silently ignoring the old server's messages, so the UI takes the product's vocabulary and the protocol keeps its history.

Revisit when: the protocol version is bumped for another reason; the tags can follow the UI names for free in that release.

### 2026-08-29, frontend-shell, the landing's live figures ride a cacheable global route

Choice: `GET /live` answers the six contributors, ten placements and locked count an open landing polls every 5s, with `public, max-age=0, s-maxage=10` and no per-visitor field in the body. `GET /landing` is unchanged: it keeps the per-IP interested block, its `no-store`, and stays the one-shot read the entry gate shares. The body is memoized for 2s behind a single-flight rebuild (`liveFigures.ts`), and the client polls only while the board is live and the tab is visible, treating a failed poll as "keep what is on screen" rather than as an outage.

Why: the figures only moved on a reload, and polling `/landing` instead is uncacheable by construction, since its body answers "has this IP opted in". Splitting the global half onto its own path is what lets an edge serve it. The keyframe snapshot could not be the source either: it is rebuilt every 5 minutes and every rebuild broadcasts the overview grid to every connected player, so speeding it up for the landing would multiply that fan-out. The memo was preferred to an in-memory activity ring buffer: the standings already need a Mongo profile lookup per rebuild, so a buffer would have removed one of the two queries and added state to keep in sync with the puzzle lifecycle, where one window bounds both.

Revisit when: the edge cache is what makes this cheap at scale and it is not armed. Cloudflare does not cache JSON on response headers alone, so until a Cache Rule marks the path eligible every poll reaches the origin, bounded only by the per-IP window (`MPP_LIVE_RATE_MAX`, 300/60s, which a reader at 5s spends 12 of). Whether a 10s edge TTL is honored at all is unmeasured on this zone: the configurable Edge TTL minimum is 2h on Free and 1h on Pro against 1s on Business, and the "use cache-control header if present" path has no documented floor. Read `cf-cache-status` once the rule exists, and fall back to a Worker or a longer TTL if the plan floors it.

### 2026-08-30, frontend-shell, the reference viewers fetch their tiles with CORS

Choice: both OpenSeadragon viewers (`ReferencePanel`, `ReferenceModal`) set `crossOriginPolicy: "Anonymous"`, so their tile requests carry an Origin, though neither ever reads a pixel back and OSD defaults to no CORS at all.

Why: the reference and the board stream the same pyramid at the same URLs, the board through `Assets.load`, which is a `fetch` WebGL leaves no choice but to make in CORS mode. R2 answers a request carrying no Origin with no `Access-Control-Allow-Origin` and no `Vary: Origin` under `max-age=14400`, so the browser replays that entry to the board's fetch and blocks it for four hours, `hydrateDziTile` retrying it every frame while the area falls back to the base level (level 11, one tile spanning 135 pieces): a deep zoom in the enlarged view left a blurry slab over exactly the area examined, and measuring prod tiles confirmed `<img>` then `fetch` fails and keeps failing where `<img crossorigin>` then `fetch` succeeds. The direction is forced rather than chosen, since WebGL cannot upload a non-CORS cross-origin image, so the board can never be the side that gives way; the thumbnail poisons nothing today (it asks for levels 9 to 10 against the board's 14 to 17) and was changed regardless, since only its width separates the two.

Revisit when: a browser already holding non-CORS entries makes the reference itself the victim for one `max-age` window, so this ships a reference that can read coarse for up to four hours for a player who used it recently. The class of bug closes at the source only with a Cloudflare Transform Rule putting `Vary: Origin` on the assets zone, which is not armed: until it is, any future non-CORS reader of an asset URL re-opens this, and no rule can repair an entry already stored.

### 2026-08-30, backend-realtime, a buried piece is not a snap target

Choice: `detectSnap` refuses a loose-loose merge whose contact piece has more than `MPP_SNAP_COVER_MAX` (5) other loose pieces standing on its centre, and the dropper gets a private `notice` toast while the drop itself rests where it landed. The count walks `GroupIndex`, resolving each covering box to the one piece of that group standing on the point (the grid is regular in solved space) so a ragged cluster's own gaps never count, and it runs only on a candidate already aligned within tolerance. The anchoring path is exempt, and so is the per-tile cap above, which stays what it is: an LOD bound, not an anti-abuse one.
Why: a stack of k pieces on one point is a bag the server searches for free, since a drop one grid step away asks "is my neighbour in this pile" and the merge is the answer. That turns a full solve from N^2 drops into N^2/k, and the per-tile cap alone allows k = 2328 on the live board (2048 cell, 120 px pieces), i.e. ~4.3e8 drops, hours of scripted play across a handful of guest connections; capping k at 5 puts it back at ~2e11. Anchoring is exempt because a locked target sits at one known position and is no bag at all, while carrying the rule there would let six pieces dumped on the frontier block the board's own progression.
Revisit when: at the densest point of the scatter band (~100 groups per world cell) the expected number of loose pieces covering a point is 0.34, so six landing on one centre by accident is ~2 pieces in a million, but that is a model of a fresh board and not a measurement of a played one. Deliberate heaping (which the movable-trays backlog item would turn into a feature) makes the refusal a routine event the toast is the only explanation for. Nothing stops the pile being built, only being used, so a griefer can still bury one contact piece for six drops; that is local, visible, and undone by dragging them off, which is why prevention was left out.

### 2026-08-30, frontend-shell, the enlarged reference aims the board, behind a mode or a modifier

Choice: a quick click on the enlarged reference moves the camera to the world point under it, but only when the player armed the crosshair toggle in the control pillar or held Ctrl (or Cmd). The modal stays open. The handler answers OpenSeadragon's `canvas-click` and sets `preventDefaultAction`, so an aimed click replaces the viewer's own click-to-zoom instead of firing alongside it; a drag (`quick` false) is never an aim. The photo is the cropped source (`cols * pieceSize` by `rows * pieceSize`), so an image pixel is already a world point and the mapping is a pure ratio, no board state involved.

Why: the reference is the one view that answers "where does this piece belong" and it could not answer "take me there", so the player read a spot off the photo and then hunted for it on the overview. Neither half of the gesture is enough alone: a bare click would take a control the viewer already uses for zoom, and a bare modifier is undiscoverable, so the toggle is what teaches the gesture and the modifier is what makes it fast once known. It stays open because the photo is what the player is reading: closing it would answer one jump and charge a reopen for the next, where the whole point of arming the mode is aiming several times off the same view. That does mean an aim gives no feedback until the modal is closed, which the toggle's own cost makes an acceptable trade and the frustum below would close properly. The mode is per-opening rather than persisted, so a modal opened to look at the photo never starts armed.

Revisit when: the reference draws no frustum, so an aimed click has no visible answer while the modal is open and a player cannot tell from the photo where they already are. That is the gap to close if the aim is used often, and it is cheap: `imageToViewerElementCoordinates` inverts the same ratio, and the stage already publishes the frustum the overview draws. The reference thumbnail was deliberately left out: at 280px it is a target the panel press would fight, and the overview beside it already answers coarse aiming.

### 2026-08-30, frontend-shell, the enlarged reference holds the image off its own edges at every zoom

Choice: the enlarged reference keeps 5% of the viewer empty on each side as OpenSeadragon viewport `margins`, re-derived from the host on every `resize` event, with `visibilityRatio: 1` and `minZoomImageRatio: 1`. Margins inset the region the image must cover, so the same number is both the fitted view's border and a hard pan limit, and `fit()` is `goHome()` now that the fitted view is itself the zoom floor.

Why: the border existed only at the zoom floor, as a `minZoomImageRatio` of 0.9 that rested the image at 90% of the viewer; the moment the player zoomed at all, `visibilityRatio: 1` pinned every image edge flush against the modal, which is where an edge is least readable and where a player examining a corner of the picture actually works. Moving the 5% from a one-off zoom overshoot to a property of the viewport makes the band identical at the fit and at 8x. Lowering `visibilityRatio` to 0.95 instead was measured and rejected: it grants the band by letting the viewport leave the content, which at the fitted zoom also lets the image be dragged until 4.5% of it is cut off the side, loosening the very framing the border exists to hold.

Revisit when: the band is a fraction of the viewer rather than of the image, so it is 5% of the modal on both axes whatever the source's shape; that is right for chrome and worth re-reading on a strongly non-square reference, which this project has not had. The panel thumbnail is untouched and needs none of this: it has no navigation at all.

### 2026-08-31, frontend-canvas, texture loads are admitted one per image-decode worker

Choice: every `Assets.load` in the canvas goes through `loadPieceTexture`, which first takes one of `TEXTURE_DECODE_SLOTS` (`navigator.hardwareConcurrency`) admission slots, handed on release to the longest-waiting caller, and starts its load deadline only once admitted so it measures the load and never the wait. `HYDRATE_MAX_INFLIGHT` is that same number, so on a board of singletons the groups in flight commit exactly what the pool can work on and nothing queues at the gate either.

Why: Pixi's `WorkerManager` runs a pool of `navigator.hardwareConcurrency` workers and dispatches its pending queue with `pop()`, last in first out, so anything handed to it past what the pool can start immediately is ordering given away: the earliest submissions sit at the bottom of that stack and are jumped by every later one for as long as the pump keeps feeding it. Since the hydrate queue is ranked nearest-to-viewport-centre first, the starved submissions were exactly the middle of the player's screen. Measured on the 1M board: 102 of one arrival's 1805 loads resolved only on the 10s deadline plus a 10s retry that re-awaited the same still-pending promise (Pixi caches one in-flight load per URL and the deadline does not cancel it), leaving the centre blank 20 seconds while the ring around it filled. The two caps are one decision because a gate deep enough to hold a backlog just moves the problem: a queue of admitted loads is frozen in submission order where the hydrate queue is re-ranked every frame, and at a commitment of 128 a pan mid-fill needed 1.5s to put a piece back at the new centre, against 0.15s once the commitment matches the pool. Neither costs throughput; the pool is the ceiling either way, ~85 loads a second.

Revisit when: Pixi dispatches that queue first-in-first-out or exposes its pool size. The slot count mirrors `navigator.hardwareConcurrency` because that is how `WorkerManager` sizes itself, so a change to either side of that coupling should bring this one with it.

### 2026-08-31, frontend-shell, a low frame rate raises a band under the topbar, read off the animation frames

Choice: `/play` raises a full-width notice under the topbar when every animation frame has taken longer than 66 ms (15 fps) for 3 unbroken seconds, measured only while the board is playable, 5s past that, and while the tab is visible. One frame back at speed clears the run, and the first slow frame starts the clock on itself rather than on the gap in front of it. The band is chrome, not an overlay: it measures itself into `--notice-h`, the stage starts under it, and `puzzleStage` follows its host with a `ResizeObserver` since Pixi's `resizeTo` watches the window alone. It carries the symptom in one line, two checks behind a disclosure, and a dismissal that is permanent per browser (`mpp.perfNoticeDismissed`).

Why: the one investigated case was hardware acceleration under Firefox, but the cause is not knowable from the page. `WEBGL_debug_renderer_info` is gated, generic or absent depending on the browser, and `PerformanceObserver`'s `long-animation-frame` is Chrome-only, so both answer for some players and for nobody in the browser this was reported on. No browser exposes a frame rate at all, which leaves the rAF gaps: a run of them, rather than an average over a window, because a run is what separates a machine that cannot draw the board (every frame slow, all session) from the expensive seconds of ordinary play (a burst of texture streaming) and from a tab the browser stopped servicing, which delivers exactly one enormous frame and then normal ones.
